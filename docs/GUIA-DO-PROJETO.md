# Guia do Projeto — Universo (fonte única de verdade)

> **Leia este documento primeiro, sempre.** Ele existe porque sessões de trabalho (humanas ou
> de IA) começam do zero com frequência, e os outros docs em `docs/` são snapshots de um
> momento específico — ficam desatualizados e nunca são o lugar certo pra confirmar "o que
> está pronto hoje". Este arquivo é o único que se compromete a refletir o estado real do
> código no momento da última revisão abaixo. Se algo aqui contradiz outro doc, este vence.
>
> **Última auditoria (comparado linha a linha com o código real):** 2026-08-27.
> **Como manter isso confiável:** depois de qualquer mudança estrutural (nova fase concluída,
> gap novo descoberto, decisão de arquitetura), atualize a seção relevante aqui na mesma PR —
> não deixe pra uma "sessão de documentação" depois, ela nunca vem.

## O que é o produto

SaaS multi-tenant de agente de IA pra WhatsApp: cada tenant (negócio) tem um agente Gemini que
responde leads reais, agenda compromissos no Google Calendar de verdade, e acompanha
pagamento/CRM. **Está em produção com um tenant pagante real** (um studio de beleza) recebendo
mensagens reais de clientes — qualquer mudança no caminho mensagem/agendamento/pagamento exige
cuidado extra (ver `CLAUDE.md` na raiz pra convenções de código e comandos).

## Mapa visual (onboarding)

`docs/mapa-do-projeto/index.html` é uma versão navegável no navegador do
conteúdo deste guia — diagramas de fluxo por área (mensagem, agenda/
financeiro, CRM/qualidade, marketing/catálogo, administração/alertas),
glossário do que cada arquivo do repositório representa, tabela de estado
real por área, pendências marcadas dentro do próprio fluxo, e (desde
02/09/2026, TASK-0215) uma seção 09 de **fluxo geral unificado** mostrando
como as cinco áreas se conectam entre si — pensada pra quem está começando
a se orientar no projeto. Abra localmente no navegador
(`file://.../docs/mapa-do-projeto/index.html`); não é servido pela
aplicação.

Também existe publicado como página web em:
**https://claude.ai/code/artifact/b7a564a3-024a-4b15-848a-c1323fbc1f4a**
— mesmo conteúdo do arquivo acima, mas acessível por link sem precisar
clonar o repositório. É um Claude Artifact vinculado à conta do dono do
produto, já com compartilhamento habilitado (qualquer um com o link
consegue abrir, não precisa estar logado na mesma conta).

Desde 02/09/2026 (TASK-0219) também existe uma segunda cópia hospedada
independentemente no Render, pra outros analistas acessarem sem depender
de conta/link do Claude:
**https://universo-mapa-arquitetura.onrender.com**
— servida como Static Site a partir de `docs/mapa-do-projeto/site/index.html`,
uma cópia derivada (não a mesma fonte do Artifact acima) com o Mermaid
carregado via CDN e inicializado no carregamento, já que a renderização de
diagrama do arquivo original depende de injeção feita pelo próprio Artifact
na hora de publicar — fora dele, os `<pre class="mermaid">` apareceriam como
texto cru. Hoje aponta pra branch `claude/gestor-refinamento-atendimento-mwu1a7`
(auto-deploy a cada commit nela), não pra `main` — reapontar pro Render se/quando
essa branch for mesclada e apagada. **Ao editar o mapa, atualize as três coisas
na mesma tarefa:** o arquivo fonte, a cópia standalone em `site/index.html`
(ou regenere-a) e republique o Artifact — do contrário os três ficam
desatualizados entre si sem aviso.

**Regra pras próximas sessões:** hoje todo editor deste repositório é uma
sessão de Claude, então editar o arquivo sem republicar o link é
considerado tarefa incompleta, não um passo opcional separado. Sempre que
mudar `docs/mapa-do-projeto/index.html` por uma mudança estrutural real do
projeto (nova fase concluída, gap fechado, feature nova — o mesmo gatilho
que já vale pra este guia), na mesma tarefa: (1) edite o arquivo local,
(2) republique o Artifact **na mesma URL acima** (nunca crie um artefato
novo pra isso — perde o link já compartilhado), (3) valide lint/testes/
build antes de commitar. É um **snapshot mantido manualmente, atualizado
por último em 02/09/2026** (inclui o PR #587/TASK-0208-0209 e a seção de
fluxo geral unificado + pendência do Microsoft Clarity, TASK-0215) — se ninguém
seguir esse passo, o link fica com conteúdo desatualizado mesmo com o
arquivo do repositório já correto. Este arquivo (`GUIA-DO-PROJETO.md`)
continua sendo a fonte de verdade em caso de conflito entre os dois.

## Arquitetura em uma tela

```
WhatsApp (Meta Cloud API / Evolution API)
        │  webhook
        ▼
server/routes/webhooks.ts ──► tenantResolver.ts (resolve tenant por phone_number_id, real DB)
        │
        ▼
server/services/autoReply.ts
        ├─ messageBuffer.ts    (agrupa rajada de mensagens, 6s, com recovery persistido)
        ├─ router Gemini/Groq  (classifica: triagem | faq | agendamento | reclamacao)
        └─ especialista Gemini (systemInstruction cacheado por tenant = Camada 1+3;
                                  contents dinâmico = Camada 4 + histórico + mensagem atual)
                │
                ├─ Google Calendar real (consultas durante geração; ações mutáveis planejadas)
        ├─ knowledgeBaseStore (documentos tipados por tenant; blob somente como fallback técnico rastreável)
                └─ conversationStore (Postgres, tenant_id obrigatório)
        │
        ▼
replySafetyGate.ts ──► aprova ou bloqueia o rascunho + plano de agenda
        │ aprovado
        ▼
executor de agenda ──► revalida disponibilidade, aplica uma única ação e só então sendBubbles

Frontend: React+Vite (src/App.tsx) ──► apiFetch real pra maioria das telas
                                    └─ SSE (/api/conversations/stream) pra atualização ao vivo
```

Todo tenant-scoped route resolve `tenantId` do JWT (`req.user.tenantId`), nunca de
body/query — ver `server/services/tenantContext.ts` e `server/middleware/rbac.ts`
(`operator < manager < admin < saas_admin`).

## Estado real por área (auditado em 2026-08-19)

| Área | Status | Detalhe |
|---|---|---|
| Schema multi-tenant (`tenants`, `operators`) | ✅ Feito | 36 migrations reais em `supabase/migrations/` |
| RLS (Row Level Security) | ✅ Efetivo em produção (26/08/2026) | Commit `2aea569` ("feat: enforce tenant RLS runtime context", direto na `main`, sem TASK-XXXX associado) trocou o backend de service key pra uma chave publicável + JWT curto assinado por requisição (`tenantDbContext.ts`, `supabaseClient.ts`), com `config.rlsEnforced` recusando subir sem `SUPABASE_PUBLISHABLE_KEY`/`SUPABASE_JWT_SECRET`. Migration `0049_rls_jwt_tenant_runtime.sql` aplicada. Rollout teve 2 incidentes reais corrigidos na sequência (TASK-0083, mesmo dia): (1) as env vars não foram configuradas no Render ANTES do merge — deploy travou em `update_failed` por ~15min até serem setadas; (2) todos os 8 jobs de fundo (`setInterval`/`startPeriodicJob`) quebraram porque rodam fora de requisição HTTP e nunca têm `TenantDbContext` — corrigido usando `getPlatformDb()` nas listagens cross-tenant e `runWithTenantDbContext({ source: 'job' })` no processamento por-tenant de cada job. Ver `docs/RLS_RUNTIME_ROLLOUT.md`. |
| 8 serviços core migrados pra Postgres por `tenant_id` | ✅ Feito | `conversationStore`, `knowledgeBaseStore`, `escalationStore`, `quickRepliesStore`, `agentStatus`, `appointmentStore`, `reminderStore`, `googleCalendar` — nenhum usa mais Map/JSON em memória |
| Base de Conhecimento tipada e versionada | ⚠️ Runtime publicado; smoke test parcial, issue #96 aberta (27/08/2026, TASK-0101/TASK-0103/TASK-0104/TASK-0105) | A migration `0057_knowledge_base_typed_documents.sql` criou documentos tipados, versões, eventos, RLS JWT e índices únicos parciais. As migrations `0058`/`0059` acrescentaram histórico `archived` e RPCs atômicos de rascunho/publicação, com tenant, papel e ator verificados no JWT. Foram preservados 24 documentos published v1 (3 tenants × 8 tipos), sem remoção de `knowledge_base.data`. A PR #459 foi integrada na `main`: o runtime compõe somente os oito documentos publicados a cada resposta, exclui rascunhos/histórico e mantém fallback rastreável ao blob caso a publicação esteja incompleta ou indisponível. Catálogo público e cópia administrativa leem a mesma fonte publicada por caminho de plataforma explícito. O smoke confirmou banco, painel e catálogo para os tenants validados; a issue permanece aberta até validar uma resposta WhatsApp de teste e a telemetria da aplicação por 24h/ciclo operacional. A interface oferece **Documentação** e uma única operação visual que carrega, salva como rascunho e publica os campos comerciais nos documentos tipados; ela não exibe JSON nem reativa o salvamento do legado. Os **Horários de atendimento** ficam em uma ação própria, pois salvam na agenda real do tenant e não na publicação comercial. |
| Roteamento de webhook multi-tenant | ✅ Feito | `tenantResolver.ts` resolve por `phone_number_id`; canal desconhecido descarta a mensagem (fail-closed), nunca cai num tenant default |
| Google Calendar por tenant | ✅ Feito | tokens OAuth em `tenant_calendar_tokens`, não mais env var global |
| Auth real + RBAC | ✅ Feito | login via bcrypt+JWT contra `operators`; `requireRole` ativo em 7 arquivos de rota |
| Onboarding de tenant | ✅ Feito (criação + ativação guiada) | `POST /api/admin/tenants` (saas_admin) além do script CLI antigo; desde TASK-0081, `TenantActivationChecklist` recalcula o checklist de ativação por tenant a partir do status real do WhatsApp, contexto do agente, horários e catálogo. Desde TASK-0301 (05/09/2026) mora dentro da aba **Empresas** (`saas_admin`), não mais numa Central "Hoje" própria — a Central foi excluída, Atendimento virou a tela padrão do sistema. A conversa de teste é recomendada e requer validação humana antes de campanha. |
| Tenant novo sem dado real mostra catálogo em branco | ✅ Corrigido (25/08/2026, TASK-0078) | Bug real: um tenant recém-criado (sem linha em `knowledge_base` ainda) mostrava o catálogo REAL da Monique (produtos, preços, dado bancário dela) no painel, como fallback silencioso do frontend (`App.tsx` usava `moniqueStudioKnowledgeBase`, cópia fixa no código). Confirmado via query no Postgres de produção que NÃO é vazamento de dado no banco (zero linhas pro tenant novo) — é fallback errado no frontend. `emptyKnowledgeBase` corrige; o catálogo de exemplo da Monique continua disponível só como preset explícito. |
| Trocar de empresa mostrava catálogo de outra empresa | ✅ Corrigido (26/08/2026, TASK-0082) | Bug real, reincidente (2 ocorrências no mesmo dia): selecionar um tenant no seletor (saas_admin) exibia o catálogo de OUTRO tenant. Confirmado nas duas vezes, via query direta no Postgres, que o banco sempre esteve correto — não é vazamento de dado nem cota do Supabase (testado via curl, resposta 401 não 402). Causa real: corrida de requisições em `App.tsx` — o efeito de `GET /api/knowledge-base` ao trocar `activeTenant.id` era o único (dos 4 efeitos por-tenant) sem proteção `cancelled` contra resposta tardia sobrescrever o estado. Corrigido pra seguir o mesmo padrão já usado em CRM/financeiro/escalonamentos. |
| Gestão de tenant (bloqueio/login/pagamento) | ⚠️ Parcial | `tenants.is_active` bloqueia login de forma reversível; o painel permite gerir credenciais de operadores; e `tenant_billing_records` guarda histórico manual mensal do Universo ao tenant. A cobrança automática continua inexistente. Desde TASK-0086, plano, assinatura, capacidades e exceções passaram a ter fonte de verdade própria; a aplicação de guardas por feature nos domínios ainda é incremental. |
| Entitlements e contrato comercial por tenant | ✅ Fundação e primeira oferta aplicadas em produção (26/08/2026, TASK-0086/TASK-0091) | A migration `0052_tenant_entitlements_foundation.sql` criou catálogo global de modelos/features/planos e relações `tenant_subscriptions`, `tenant_feature_overrides`, `tenant_feature_usage` e `tenant_entitlement_audit`, com RLS JWT em todas as tabelas tenant-scoped e negação explícita das globais para API pública. A TASK-0091 adicionou os planos **Essencial** (Gs. 349.000/mês) e **Profissional** (Gs. 649.000/mês), ambos com 13 regras explícitas; preservou os tenants existentes em Compatibilidade integral v1; e criou o piloto inativo `piloto-comercial-essencial`, sem canal nem operador. `GET /api/me/entitlements` usa apenas o tenant do JWT; `saas_admin` gerencia plano e exceções auditáveis pelo painel. Falta conectar guardas de feature gradualmente em cada domínio de negócio. |
| Frontend saindo do localStorage | ✅ Feito | `App.tsx` já faz chamadas reais de API pra quase tudo. **`FinancialDashboard.tsx` foi ligado à API real em 19/08/2026** (`financial.ts`/`financialStore.ts`, migration `financial_transactions`): KPIs e gráficos derivados 100% de transação real, sem número fabricado, e confirmar um comprovante no WhatsApp (card de Escalonamentos) cria a transação sozinho — ver `recordFinancialTransactionForVerifiedPayment` em `conversations.ts`. **Achado real (26/08/2026, TASK-0097): `FinancialDashboard.tsx` nunca é renderizado em lugar nenhum** — a aba "Financeiro" que o usuário vê de verdade é `AgendaFinanceiroCenter.tsx` (`scope="financial"`), com sua própria tabela/UI de transações; `FinancialDashboard.tsx` só aparece hoje num comentário de `App.tsx`. `CLAUDE.md` corrigido pra apontar isso explicitamente. **`OperatorCRM.tsx` também já estava ligado à API real** (`crm.ts`/`crmStore.ts`, migration `crm_lead_state`) desde antes do gap ter sido documentado errado como "100% mock" — o único problema era um banner de aviso desatualizado, removido em 20/08/2026. Nessa mesma data o CRM ganhou a ponte com o Financeiro: marcar um lead como "ganho" com valor negociado cria uma cobrança `pendente` automaticamente (`recordFinancialTransactionForWonDeal` em `crm.ts`, dedupe por `source_ref` igual ao padrão de `verify-payment`). Ainda falta gateway de pagamento real (fica registro-only por decisão de escopo). Desde 26/08/2026 (TASK-0097) o Financeiro também tem **despesas recorrentes**: cadastro único (descrição, valor, forma, dia do mês 1-28) em `AgendaFinanceiroCenter.tsx`, gerado automaticamente todo mês pelo job `recurringExpenseJob.ts` (`recurring_expenses`, migration `0056`, RLS JWT), idempotente por `last_generated_month` + `source_ref` único. |
| Central de Qualidade e Aprendizado | ✅ Código + migration aplicados | `QualityAuditCenter.tsx` oferece revisão de sugestões da IA, bugs, ideias de operadores, recomendações e linha do tempo. `qualityAuditStore.ts` e `qualityAudit.ts` mantêm o feedback separado por tenant. O webhook de comprovantes e as decisões humanas de pagamento geram eventos auditáveis. A migration `0040_quality_audit_learning.sql` foi aplicada no projeto Supabase de produção e confirmada via REST: `quality_reviews` e `quality_audit_events` respondem HTTP 200. **Continua sendo só "evidência para revisão humana; não é uma regra automática" (comentário explícito no código)** — não altera o agente sozinha; ver linha "Aprovar e enviar rascunho bloqueado" abaixo pro ciclo que de fato realimenta o agente. |
| Aprovar e enviar rascunho bloqueado (Escalonamentos) | ✅ Feito (26/08/2026, TASK-0093) | Quando o revisor pré-envio bloqueia uma resposta da IA, o rascunho já ficava salvo (agora em `escalations.blocked_draft`, estruturado, migration `0054`) e "Gerar sugestão" já usava esse rascunho — mas só dava pra Copiar/Descartar, nunca enviar, e nada realimentava o agente. Agora tem botão **"Aprovar e enviar"**: manda pelo mesmo caminho real do operador (`POST /api/conversations/:phone/send`, desabilitado se a janela de 24h da Meta já fechou) e só então resolve o caso e grava um "exemplo aprovado" (`tenant_approved_reply_examples`, RLS JWT) — nunca disponível pra `payment_proof`. Os últimos 3 exemplos aprovados do tenant entram no contexto dinâmico (Camada 4, nunca cacheado) das próximas mensagens parecidas, via `autoReply.ts`. Nunca reescreve a Base de Conhecimento sozinho; reversível (basta apagar a linha). Também corrigido: escalonamentos arquivados sumiam de vez da interface (`EscalationsPanel.tsx` filtrava `status !== 'archived'` sem exceção) — agora tem aba **Arquivados** + botão Restaurar. **Bug real achado no mesmo dia usando a própria feature (TASK-0096):** "Gerar sugestão" (`replySafetyGate.ts`) respondeu em português numa conversa em espanhol e descartou um preço já confirmado no catálogo — o prompt tinha a regra de idioma mas nada validava o resultado (diferente do rascunho original do agente, que já validava via `hasSpanishSignal`/`hasPortugueseSignal`). Corrigido: prompt reforçado pra preservar dado confirmado, e nova validação pós-geração que descarta a sugestão (tenta o outro provedor, senão devolve 503 mantendo o bloqueio) se o idioma sair errado. |
| Idempotência de webhook | ✅ Feito | Postgres (`processed_webhook_messages`), sobrevive restart/multi-instância — **doc antigo dizia "em memória", isso está errado, já foi corrigido no código** |
| Buffer de rajada de mensagens | ✅ Feito, com recovery | Map em memória é o caminho rápido, mas cada rajada é persistida (`pending_message_buffers`) e um sweeper recupera se a instância reiniciar no meio da janela |
| SSE de conversas (`conversationEvents.ts`) | ❌ Gap real | `EventEmitter` em memória, single-instance, não sobrevive restart — só tem um poll de 90s como rede de segurança. Vira problema no dia que escalar horizontalmente. |
| Custo de infraestrutura (egress do Supabase) | ⚠️ Mitigado no código (ETag, 25/08) + causa raiz estrutural em migração (TASK-0218, 02/09/2026, em andamento) | A organização Supabase (plano Free) estourou 403% do limite mensal de saída (20GB usados / 5GB) e entrou em restrição — causa raiz: `GET /api/knowledge-base` mandava o objeto inteiro (chegou a ~12MB numa tenant, quase tudo foto de produto em base64) toda vez que o painel recarregava, mesmo sem lead real. Corrigido cache condicional por ETag nessa rota (304 sem corpo quando o dado não mudou) — mitigação de payload, não elimina a causa raiz. **TASK-0218 ataca a causa raiz de verdade**: as fotos de produto/variante/antes-depois/1º-contato saem do jsonb `knowledge_base` e vão pro Supabase Storage (mesmo padrão já usado por vídeo, `server/services/knowledgeBaseImageStore.ts`), com fallback de leitura pro Base64 legado durante a transição — ver registro "TASK-0218" em "Mudanças recentes relevantes" abaixo. Backend/runtime/frontend/testes de segurança/script de migração já implementados (7 commits na branch); **falta rodar o dry-run real do script contra produção e medir o ganho real por tenant** antes de considerar o incidente encerrado. Auditoria por tamanho real de tabela (`pg_total_relation_size`) confirmou que `knowledge_base` (27MB) era disparado o maior consumidor. |
| Catálogo público por tenant | ✅ Feito (migration aplicada, dado corrigido) | `/catalogo/:slug` lê produtos ativos e preços vigentes da `knowledge_base`, expõe somente campos comerciais e mantém regras internas fora do payload. Migration `0042_public_catalog.sql` aplicada em produção (23/08). **Bug real corrigido (23/08):** o opt-in/contato da Monique tinha sido gravado via SQL direto no tenant errado (`11111111-...`, renomeado pra "Clic Piscinas (Meta)" em 18/08) em vez do tenant real (`8a786c2a-...`, slug `monique-teste`) — corrigido, e uma aba **Catálogo** nova no painel (`PublicCatalogSettings.tsx` + `GET/PUT /api/public-catalog-settings`, escopado por `tenantOf(req)`) elimina essa classe de erro daqui pra frente. Endpoint usa rate limit próprio. |
| Pagamentos | ❌ Não existe (gateway) | Nenhum provedor real (Stripe/PIX/etc). Hoje é um campo `payment_status` que um operador humano marca manualmente (card de Escalonamentos, kind `payment_proof`), com a IA lendo o comprovante só como dica, nunca confirmando sozinha. Desde 19/08/2026 essa confirmação já vira um registro real no Financeiro (`financial_transactions`), mas continua sendo *registro*, não *cobrança* — nada aqui move dinheiro de verdade. |
| Observabilidade / error tracking | ⚠️ Parcial — sem serviço externo, mas com painel admin real desde 27/08/2026 (doc anterior estava desatualizada, não mencionava isto) | Ainda zero Sentry/Pino/serviço externo (decisão deliberada). Dois canais de **alerta ativo** (push + WhatsApp template pro `admin_alert_phone`): erro de sistema genérico (`systemErrorAlertService.ts`, issue #111) e sessão Baileys/Evolution caindo (`evolutionConnectionAlertJob.ts`, 24/08/2026). Separado disso, desde 27/08/2026 (commit `e2892b0`, "feat: adiciona logs técnicos auditáveis por tenant") existe um painel admin real de **visibilidade sem alerta**: `logStructured`/`withStructuredLog` (`server/services/structuredLog.ts`) já instrumenta `autoReply.ts`, `googleCalendar.ts`, `appointmentStore.ts` e `webhooks.ts`, gravando falhas como `system_incidents` (Postgres, RLS por tenant, dedupe por `sourceKey`, com contagem de ocorrências e workflow revisar/resolver/arquivar) exibidos em **Logs do Sistema** (`SystemLogsPanel.tsx`, `GET/POST /api/system-incidents*`). Não é alerta proativo (o admin precisa abrir a tela), mas já tira "grep manual no Render" da única forma de descobrir um erro. TASK-0196 (01/09/2026) ensinou a classificação a reconhecer o esgotamento de cota/crédito pré-pago do Gemini (`RESOURCE_EXHAUSTED`/"prepayment credits"/"quota exceeded") como incidente `critical` + categoria `integration` com ação sugerida explícita — antes caía como `medium` genérico, indistinguível de qualquer erro isolado, apesar de ser o incidente documentado como mais recorrente e mais grave (derruba TODAS as chamadas Gemini do projeto, todos os tenants, ao mesmo tempo). Cobertura ainda não chega a `server/gemini.ts` fora desses 4 call sites, nem a fluxos de pagamento/escalonamento/`googleSheetsSync.ts` — ver gap #3 abaixo. |
| Testes & CI | ✅ Feito | 190 arquivos de teste e 995 testes na validação local da TASK-0102 em 26/08/2026; `.github/workflows/ci.yml` roda lint+test+build em toda PR |
| Envio real de WhatsApp | ✅ Feito | Meta Cloud API (`metaSend.ts`) e Evolution API (`evolutionSend.ts`) ambos batem em endpoints reais |

## Gaps conhecidos que valem engenharia (priorizados)

1. **SSE single-instance** (`conversationEvents.ts`) — bloqueia escalar horizontalmente sem perder atualização em tempo real pra parte dos usuários. Fix: Postgres `LISTEN/NOTIFY` ou Redis pub/sub.
2. ~~`FinancialDashboard.tsx` mockado sem aviso~~ / ~~`OperatorCRM.tsx` mockado / sem ponte com o Financeiro~~ — ambos resolvidos, ver linha "Frontend saindo do localStorage" acima.
3. **Observabilidade ainda é visibilidade passiva, não alerta ativo** — desde 27/08/2026 existe `system_incidents`/**Logs do Sistema** (ver linha "Observabilidade" acima), e desde TASK-0196 (01/09/2026) o esgotamento de cota do Gemini já é classificado como `critical`, mas o admin só descobre abrindo a tela — ninguém é avisado sozinho, diferente dos dois casos que já têm push+WhatsApp (`systemErrorAlertService.ts`/`evolutionConnectionAlertJob.ts`). Fix concreto pro próximo passo: ou um alerta ativo específico pra incidentes `critical` (reaproveitando `adminAlertChannel.ts`), ou pelo menos expandir a cobertura de `withStructuredLog` pra fora de `autoReply`/`googleCalendar`/`appointmentStore`/`webhooks` (faltam `server/gemini.ts` direto, pagamento, escalonamento, `googleSheetsSync.ts`).
4. ~~**RLS não é a barreira real de isolamento**~~ — resolvido em 26/08/2026 (commit `2aea569` + TASK-0083). RLS agora é a barreira ativa (chave publicável + JWT curto por tenant), não só disciplina de código.
5. ~~**Catálogo público e troca de endereço**~~ — migration aplicada, dado corrigido pro tenant certo (`monique-teste`), aba de edição no painel adicionada, fonte/cor corrigidas (o texto ficava "fantasma" no Chrome Android — causa raiz e fix em "Mudanças recentes" 24/08) e **confirmado visualmente pelo dono do produto em produção**. Mensagem pré-preenchida do botão de WhatsApp também virou editável pelo painel (antes era texto fixo no código). Só falta a decisão de negócio de trocar o link usado em anúncios/bio pela Netlify pelo `/catalogo/monique-teste` — não é mais bloqueio técnico.
6. **Pagamento sem provedor real** — bloqueia cobrar cliente novo de forma automática. **Decisão de provedor tomada em 02/09/2026 (ainda sem implementação):** Stripe descartado — não está entre os países suportados pro Paraguai abrir conta comerciante (exigiria constituir entidade nos EUA só pra isso); **Pagopar** escolhido em vez de Bancard direto ou dLocal, por já ter um produto pronto de assinatura/cobrança recorrente (débito automático + retry) rodando sobre a rede da própria Bancard, sem precisar virar comerciante Bancard direto (mais KYC bancário) nem lidar com a complexidade cross-border do dLocal (pensado pra empresa estrangeira sem presença local, não é o caso aqui). Implementação **explicitamente adiada pro futuro** (pedido do dono do produto) — falta abrir a conta comerciante na Pagopar (RUC + dados bancários) antes de qualquer código; quando isso acontecer, entra como um provider de pagamento novo (`server/services/`, migration com tabela de assinatura/cobrança, webhook de confirmação), seguindo o mesmo padrão de isolamento por tenant do resto do sistema.
7. ~~**Central de Qualidade aguardando migration 0040**~~ — migration aplicada no Supabase de produção e tabelas confirmadas via REST; continua pendente apenas a validação funcional ponta a ponta após o deploy do PR.

## Mudanças recentes relevantes (não é auditoria completa, só registro)

**05/09/2026 (TASK-0301 — Atendimento vira a tela padrão, CRM/Agenda/Financeiro saem do menu superior):**
- Pedido direto do dono do produto: o menu superior (`Header.tsx`) foi enxugado — "Hoje" e "Conversas" saíram (Atendimento agora é a tela padrão do sistema, sem botão próprio; o logo "Universo" virou clicável e leva de volta pra lá), e CRM (Vendas), Agenda e Financeiro também saíram do topo. Menu superior fica só com Crescimento, Configurar e Empresas.
- CRM/Agenda/Financeiro só ficam acessíveis agora pela caixa de ferramentas ("Ferramentas", gaveta mobile dentro do Atendimento) — novas props `onGoToCrm`/`onGoToFinancial` em `WhatsAppLeadsSim.tsx`, mesmo padrão já existente de `onGoToAgenda`/`onGoToEscalations`. Só mobile por enquanto (confirmado com o dono do produto) — desktop não tem gaveta de Ferramentas (removida na TASK-0225).
- A Central "Hoje" (`OperationsHomeWorkspace`/`OperationsCenter`, seções Prioridades/Atalhos/Configurar) foi excluída por completo — Prioridades e Atalhos por pedido direto ("vamos refinar isso futuramente"); só a etapa "Configurar" (checklist de ativação do tenant, `TenantActivationChecklist`) sobrevive, agora dentro da aba Empresas, visível só a quem já enxerga essa aba (`saas_admin`) — deixou de estar disponível pra um admin de tenant que não seja saas_admin, aceito explicitamente como limitação temporária.
- "home" saiu do tipo `ActiveTab` — `parseStoredActiveTab` (`activeTab.ts`) migra qualquer preferência antiga salva (`home`) pra `whatsapp`, mesmo padrão já usado pra `integration`/`agenda_financeiro`.
- Auditoria à parte (sem mudança de código) do menu ⋮ de uma conversa aberta: todos os 12 itens conferidos ponta a ponta até o backend, incluindo "Devolver a IA agora" (`operator_ai_release_at` → `webhooks.ts` pula a pausa de 5min pós-resposta manual) — nenhum bug de fiação encontrado. Único ponto de atenção: "Sugerir melhoria"/"Reportar bug" dependem do módulo Qualidade estar ligado pro tenant (`requireQualityModule()`).
- Implementação em `src/types.ts`, `src/lib/activeTab.ts`, `src/App.tsx`, `src/components/Header.tsx`, `src/components/WhatsAppLeadsSim.tsx`; excluídos `OperationsHomeWorkspace.tsx`/`OperationsCenter.tsx`. Validação local: lint, suíte com 270 arquivos/1486 testes e build concluídos sem erro. Validação visual real pendente (rede externa deste ambiente segue bloqueada).

**02/09/2026 (TASK-0218 — migração das imagens inline Base64 da Base de Conhecimento pro Storage, em andamento):**
- Ataca a causa raiz confirmada do incidente de egress TASK-0074/0075 (ver linha "Custo de infraestrutura" acima): fotos de produto/variante/antes-depois/bloco de 1º contato saem do jsonb `knowledge_base` e vão pro Supabase Storage, mesmo padrão já usado por vídeo (`kb-image/{tenantId}/{imageId}` no bucket `app-data`, sem tabela de metadados separada — mimeType/fileName/sizeBytes ficam inline no JSON ao lado do id).
- Contrato de compatibilidade obrigatório durante a transição (`resolveKnowledgeBaseImageBinary` em `knowledgeBaseImageStore.ts`): existe `*ImageId` → usa Storage; senão existe `*Base64` legado → usa e loga observabilidade (`source=legacy_base64_fallback`); senão → imagem ausente. Os 3 pontos que mandam foto real por WhatsApp (`autoReply.ts` `runMidiaTool`, `firstContactMessage.ts`, `conversations.ts` `send-example-photo`), o catálogo público, o PDF do catálogo e o editor da KB (`AgentKnowledgeBase.tsx`) já usam esse contrato.
- Rotas novas `POST/GET /api/knowledge-base/images*` (upload/download autenticado, tenant sempre resolvido do JWT — nunca de body/query) com limpeza de órfão no save real (mesmo padrão da Issue #261, já existente pra vídeo) — cobertos por 5 testes de isolamento cross-tenant (`conversationsKnowledgeBaseImageIsolation.test.ts`) que provam, com um "Storage" fake chaveado por `(tenantId, imageId)`, que um tenant nunca alcança a imagem de outro mesmo sabendo o id exato.
- Script `scripts/migrate-knowledge-base-images.ts`: dry-run por padrão, `--apply`/`--tenant`/`--limit`/`--remove-legacy-base64` (a remoção do Base64 legado é sempre um passo separado e deliberado, nunca automática).
- **Status real (não confie só nesta linha, ver `docs/task-registry/TASK-0218.md` pra detalhe por etapa):** 7 commits na branch `claude/agente-atendimentos-refinement-vg5wtl`, `npm run lint`/`npx vitest run`/`npm run build` passam limpo a cada etapa. **Ainda não executado:** dry-run real do script contra o Supabase de produção (nenhuma migração de dado real rodou ainda), validação manual no navegador do fluxo de upload e do PDF do catálogo. PR ainda não aberto até o momento deste registro.

**01/09/2026 (TASK-0193/TASK-0196 — Ficha IA repetindo informação + observabilidade: doc desatualizada e esgotamento do Gemini invisível):**
- Dois achados reais reportados pelo dono do produto na mesma conversa (via prints de uso real). **TASK-0193:** um rascunho gerado pela Ficha IA (`POST /api/ai/reply-from-hint`) re-cumprimentou e repetiu um preço já enviado poucas mensagens antes — confirmado como bug real (o prompt desse endpoint é separado do `autoReply.ts` e não tinha as regras 6/9 do bloco `REGRAS DE ESTILO`, agora adicionadas).
- **TASK-0180 → cancelada:** ao investigar "a IA não voltou a responder depois que o operador respondeu manualmente", descobri no meio da implementação que a TASK-0181 já tinha resolvido o mesmo achado de forma independente, já mesclada em produção — auditei a implementação deles (não só confiei no "concluído") antes de aceitar como resolvido. Limpo um resíduo real: uma migration minha aplicada em produção antes da descoberta ficou órfã (`operator_released_ai_at`, sem código nem arquivo no repo) — removida.
- **TASK-0196 (item 1 da avaliação de maturidade do agente, "observabilidade"):** ao escopar esse item, encontrei que a linha "Observabilidade" desta tabela estava **desatualizada** — dizia "❌ Não existe (geral)" sem mencionar `system_incidents`/`SystemLogsPanel.tsx` (painel real de Logs do Sistema, existe desde 27/08/2026, commit `e2892b0`). Corrigido aqui. Como primeiro recorte concreto (reaproveitando 100% da infra existente, sem tabela nova nem UI nova), `getSystemIncidentFromStructuredLog` (`structuredLog.ts`) passou a reconhecer o padrão de erro do esgotamento de cota/crédito pré-pago do Gemini (`RESOURCE_EXHAUSTED`/"prepayment credits"/"quota exceeded") como incidente `critical` + `integration`, com ação sugerida apontando pra `ai.studio/projects` — antes esse incidente (documentado no CLAUDE.md como recorrente e o mais grave possível, derruba a IA inteira pra todos os tenants ao mesmo tempo) caía como `medium` genérico, indistinguível de qualquer erro isolado de um tenant só. Gap real que continua: cobertura de `withStructuredLog` ainda não chega a `server/gemini.ts` fora dos 4 call sites já instrumentados, e mesmo um incidente `critical` só aparece se alguém abrir o painel — não empurra alerta como os dois casos que já têm push+WhatsApp.
- Validações locais: TASK-0193/0194 com testes novos + disciplina de regressão (revert do fix, confirma teste falha, restaura, confirma que passa) para cada um; lint, suíte completa e build limpos em ambos.

**27/08/2026 (Operação visual única da Base de Conhecimento):**
- A antiga visualização técnica dos documentos tipados foi substituída pelo formulário visual já conhecido de perfil, regras, catálogo, variações, mídias, FAQ, anexos e primeiro contato. O formulário busca a versão publicada ou o rascunho existente dos documentos tipados e, ao salvar, grava somente os seis documentos editáveis correspondentes; ele não executa `POST /api/knowledge-base` nem cria uma segunda fonte de dados.
- A ação **Publicar alterações** é separada do salvamento de rascunho e exige confirmação. O resumo superior informa o número de documentos publicados e de rascunhos pendentes. O histórico tipado continua sendo a referência auditável de versões; `knowledge_base.data` permanece somente como fallback técnico temporário enquanto a issue #96 não é encerrada.
- Anexos novos usam uma rota de Storage desacoplada do blob e só são associados ao documento `media_assets` quando o rascunho visual é salvo. Remoções feitas no rascunho não apagam a mídia ainda publicada.
- **Horários de atendimento** saíram de Perfil & Objetivo e passaram a ter botão próprio no topo, abrindo um painel independente. O salvamento continua em `tenants.business_hours`, fonte usada pela agenda real, sem depender de rascunho ou publicação comercial.

**27/08/2026 (Documentação da Base de Conhecimento):**
- A área **Agente & catálogo → Base de Conhecimento** ganhou um botão de **Documentação** no cabeçalho. Ele abre uma leitura separada com os oito documentos tipados, o fluxo `assunto → rascunho → revisão → publicação → próxima resposta` e o caminho de proteção quando a publicação não está completa.
- A documentação declara explicitamente os blocos ainda reservados (`opening_hours` e `human_handoff_rules`), aponta os controles que continuam fora deles nesta versão e explica que o editor legado é somente auditoria/rollback quando a fonte publicada está ativa. Ela não altera conteúdo, publicação, permissões, catálogo, mídia ou dados de qualquer tenant.
- A interface continua preservando todos os controles da Base de Conhecimento: diagnóstico, modelos/cópia (quando permitido), Camada 1 por tenant, painel de documentos tipados, catálogo, regras, FAQ, documentos, horários e mensagem inicial. Validações locais: lint, 199 arquivos/1.037 testes e build concluídos.

**26/08/2026 (TASK-0102 — revisor pré-envio antes de efeitos de agenda):**
- Uma auditoria autorizada do fluxo de agendamento encontrou uma condição crítica: o agente criava uma reserva temporária e somente depois submetia a mensagem de confirmação ao revisor pré-envio. Se o revisor bloqueasse o texto, o hold poderia permanecer sem aviso para a cliente.
- O fluxo agora separa **planejamento** de **execução**. `criar_agendamento`, `remarcar_agendamento`, `cancelar_agendamento` e `criar_pre_reserva` ficam pendentes durante a geração; o revisor recebe o texto e o resumo do plano, explicitamente marcado como ainda não executado. Com bloqueio, o fluxo encerra sem efeito mutável. Com aprovação, o executor aplica no máximo uma ação e confere a disponibilidade novamente antes do envio.
- Se a execução aprovada falhar, o rascunho afirmativo não é enviado e o caso é escalado. Se o envio falhar depois de criar um hold identificável deste próprio turno, ele é liberado por melhor esforço; pré-reserva recém-criada é marcada como cancelada. Remarcações e cancelamentos não são revertidos automaticamente após possível entrega parcial e seguem para revisão humana.
- Cobertura adicionada para: geração sem mutação, execução pós-aprovação, contexto levado ao revisor, compensação de hold e regras anteriores de duração, duplicidade e cancelamento. Validações locais: lint, 190 arquivos/995 testes e build aprovados. Não houve nova mutação de agenda em produção durante essa validação.

**26/08/2026 (TASK-0100 — identidade canônica de celular brasileiro no Evolution):**
- Um teste prático autorizado no tenant Monique confirmou o envio Evolution até o aparelho e a resposta até o banco. O painel parecia não receber a resposta porque a Evolution devolveu o mesmo celular brasileiro em formato legado, sem o nono dígito móvel, criando uma segunda conversa para o mesmo contato.
- `webhookParsers.ts` agora converte esse caso antes de resolver tenant, gravar mensagem ou rodar automação: somente números brasileiros `55 + DDD + assinante móvel de oito dígitos iniciado entre 6 e 9` recebem o nono dígito; linhas fixas e números de outros países não mudam. A correção é prospectiva e cobre também o fallback `@lid` da Evolution.
- As conversas históricas criadas durante o teste não foram fundidas automaticamente: mensagens e etiquetas existentes seguem intactas até haver uma decisão explícita para consolidação de dados em produção.

**26/08/2026 (TASK-0091 — planos comerciais e página pública de oferta):**
- Foram aplicadas em produção as migrations `0054_commercial_offer_plans_and_pilot.sql` e `0055_commercial_interest_requests.sql`. Elas materializam os planos Essencial (Gs. 349.000/mês) e Profissional (Gs. 649.000/mês), com 13 regras de feature cada, e uma tabela privada para contatos que manifestam interesse comercial.
- O tenant **Piloto Comercial Essencial** foi provisionado intencionalmente como `is_active=false`, com assinatura `trial` no Essencial e sem telefone, token, operador, canal ou dados de clientes. Nenhum tenant em operação foi alterado.
- A nova página pública `/oferta` busca somente o catálogo comercial publicável, compara os dois planos e encaminha interesse a uma rota de servidor validada, com consentimento obrigatório e limite de 8 envios por IP/hora. Ela declara explicitamente que não existe cobrança automática. O advisor de segurança do Supabase voltou sem alertas após as migrations.
- A preparação comercial está pronta; a ativação do piloto depende de criar seu canal e primeiro operador em procedimento separado, seguido de uma conversa de teste humana. A adoção dos guardas de feature por domínio continua incremental e deve iniciar no backend.

**26/08/2026 (TASK-0086 — fundação de entitlements):**
- O produto deixou de depender de campos decorativos de plano e de `segment` para representar contrato comercial. A migration `0052_tenant_entitlements_foundation.sql` foi aplicada e rastreada no Supabase de produção, criando catálogo de features, planos versionados, regras de plano, assinaturas, overrides com expiração, uso e auditoria.
- Todos os tenants existentes receberam **Compatibilidade integral v1**, com 13 capacidades habilitadas, preservando o comportamento anterior durante a transição. O registro de validação confirmou 3 tenants e 3 assinaturas correntes; o advisor de segurança do Supabase não retornou alertas após a migration.
- `featureEntitlementService.ts` resolve a decisão efetiva (plano → exceção ativa → uso), e o painel SaaS permite visualizar e administrar a decisão por tenant. O tenant autenticado pode consultar somente `GET /api/me/entitlements`; rotas de alteração permanecem exclusivas de `saas_admin`.
- **Correção pré-merge de RLS (PR #435):** uma revisão encontrou que a leitura autenticada de entitlements ainda usava `getPlatformDb()`. A migration `0053_tenant_entitlements_runtime_read_rls.sql` foi aplicada em produção e o serviço foi separado: `GET /api/me/entitlements` deriva o tenant do `TenantDbContext` e usa `getDb()` sob JWT/RLS; apenas a rota `saas_admin` cross-tenant permanece no cliente de plataforma. O advisor de segurança voltou sem alertas e há teste de regressão contra fallback para `getPlatformDb()`.
- **Limite deliberado:** esta entrega estabelece a fonte de verdade e os controles, mas não bloqueia retroativamente cada endpoint/tela de domínio por feature. A próxima fase deve integrar guardas por domínio com telemetria, sem usar `segment` como permissão.
- **27/08/2026 (TASK-0108 — central única de controles):** o modal SaaS de contrato e capacidades foi condensado em uma página responsiva. O administrador escolhe um recurso em uma grade compacta por domínio e usa um único painel para liberar, bloquear ou limitar a capacidade, sempre com motivo obrigatório, expiração opcional e trilha de auditoria. `admin.tenant_operators` é destacado para controlar a permissão e o limite de operadores do tenant. Esse controle não bloqueia individualmente um usuário já existente; esse tipo de gestão exigiria um modelo próprio de membros por tenant.


**26/08/2026 (TASK-0084 — navegação operacional e separação de Agenda/Financeiro):**
- A barra principal foi reorganizada por intenção de trabalho: **Hoje**, **Conversas**, **Vendas**, **Agenda**, **Financeiro** e, para administradores, **Crescimento**. Agenda fica imediatamente após Vendas para acompanhar o fluxo comercial até o horário confirmado.
- **Agenda** e **Financeiro** deixaram de ser uma tela combinada. As duas superfícies reaproveitam os mesmos dados e ações já validados, mas cada uma mostra só sua rotina: calendário e próximos atendimentos na Agenda; receitas, despesas e cobranças em aberto no Financeiro.
- A preferência persistida `agenda_financeiro` é migrada automaticamente para `agenda`, sem levar usuários a uma rota inexistente após atualização.
- Configurações administrativas foram agrupadas em **Configurar** (Agente & catálogo, Catálogo público e Qualidade do agente); **Plataforma** contém somente Empresas para `saas_admin`. A tela de Pendências continua acessível por atalhos operacionais, sem competir com a navegação principal.
- Implementação em `Header.tsx`, `AgendaFinanceiroCenter.tsx`, `App.tsx`, `OperationsCenter.tsx` e `activeTab.ts`, com atualização de testes. Validações locais: lint, suíte com 182 arquivos/948 testes e build concluídos sem erro.

**26/08/2026 (TASK-0083 — RLS efetivo quebrava boot e jobs de fundo):**
- Commit `2aea569` ("feat: enforce tenant RLS runtime context", direto na `main`, sem TASK-XXXX/PR — origem fora do fluxo normal de sessão) endureceu o RLS: `getDb()` passou a exigir um `TenantDbContext` (chave publicável + JWT curto assinado, não mais service key) e a recusar rodar sem `SUPABASE_PUBLISHABLE_KEY`/`SUPABASE_JWT_SECRET` configuradas. Duas quebras reais em produção na sequência, ambas confirmadas por log real (`mcp__Render__list_logs`), não suspeita:
  1. **Boot travado (~15min):** as duas env vars não foram configuradas no Render ANTES do merge — `loadConfig` lançava e o processo saía (`Application exited early`), deploy ficava em `update_failed`. Site continuou no ar (zero-downtime do Render manteve a build antiga), mas nem o RLS nem um PR seguinte (#428, TASK-0082) chegaram a produção até as variáveis serem configuradas (`SUPABASE_PUBLISHABLE_KEY` via `mcp__Supabase__get_publishable_keys`; `SUPABASE_JWT_SECRET` só o dono do produto tinha acesso, veio do Supabase Dashboard → API Keys → JWT Keys) e um novo deploy disparado.
  2. **8 jobs de fundo quebrados:** `setInterval`/`startPeriodicJob` nunca rodam dentro de uma requisição HTTP, então nunca têm `TenantDbContext` do `AsyncLocalStorage` — `getDb()` chamado por eles (direto ou via store) sempre lançava `Acesso ao banco sem contexto de tenant`. Afetava silenciosamente: alerta de agente pausado, alerta de conexão WhatsApp, lembretes de agendamento, alerta de pagamento pendente, acompanhamento de funil, follow-up de pré-reserva, expiração de reserva retida e o sweeper de recuperação de buffer de mensagens — justamente os jobs de segurança que existem por causa de incidentes reais anteriores (ex: agente pausado 4h sem alerta, issue #115). `tenantDbContext.ts` já previa isso (`source: 'job'`), mas nenhum job usava. Corrigido: listagens cross-tenant (`listTenantIdsWith...`, `listConnectedCalendarTenants`, etc.) usam `getPlatformDb()`; processamento por-tenant de cada job roda dentro de `runWithTenantDbContext({ tenantId, source: 'job' })`, preservando isolamento por tenant mesmo fora de uma requisição.
- Ver `docs/task-registry/TASK-0083.md` e `docs/RLS_RUNTIME_ROLLOUT.md` (checklist de rollout seguro, já existente antes desta correção).

**26/08/2026 (TASK-0081 — ativação guiada do tenant):**
- A Central **Hoje** passou a exibir uma jornada objetiva de ativação baseada em fontes reais, não em um flag manual: conexão WhatsApp, contexto comercial do agente, agenda/horários e catálogo com preço e duração estruturados.
- Se alguma configuração crítica for removida, a pendência reaparece automaticamente; nenhum tenant fica marcado como pronto por um estado antigo de interface.
- A conversa de teste aparece como etapa recomendada, mas deliberadamente não é marcada como concluída pelo sistema: validar resposta e próximo passo num número real continua sendo decisão humana antes de ativar uma campanha.
- Implementação em `src/lib/tenantActivation.ts`, `src/components/TenantActivationChecklist.tsx`, `src/components/OperationsCenter.tsx` e `src/App.tsx`, com três novos testes unitários. Validação local: `npm run lint`, `npx vitest run` (181 arquivos/943 testes) e `npm run build` concluídos sem erro.



**24/08/2026 (investigação Lucas Gimenes/tenant Monique — conexão WhatsApp):**
- **Bug real investigado:** cliente mandou mensagem de texto pro número comercial, ela apareceu entregue no WhatsApp dele (checkmarks normais), mas nunca chegou no painel nem gerou resposta da IA ("Contexto supervisionado" ficou vazio). Causa: a sessão Baileys/Evolution desse tenant caiu — o WhatsApp continua entregando mensagem ponta-a-ponta entre os dois números (não depende da nossa conexão), mas com a sessão caída **nenhum webhook chega no backend**, nem a mensagem do lead nem o eco do que o operador manda direto do celular (`fromMe: true`, `server/routes/webhooks.ts`). Ninguém percebeu na hora porque `GET .../evolution-instance/status` (`admin.ts`) só é consultado quando alguém abre o modal de reconexão no painel — não existia nenhum monitoramento de fundo.
  - **Fix:** novo job `server/services/evolutionConnectionAlertJob.ts` (mesmo padrão de `agentPausedAlertJob.ts`/`systemErrorAlertService.ts`), consulta `connectionState` de cada `tenant_evolution_credentials` a cada 5min e alerta (push + template WhatsApp pro `admin_alert_phone`) quando fica fora de `"open"` por mais de 5min — só uma vez por episódio de queda, nunca reconecta sozinho. Colunas novas em `tenant_evolution_credentials`: `last_connection_state`, `disconnected_since`, `disconnected_alert_sent_at` (migration `0039_evolution_connection_health.sql`). **Precisa do template `whatsapp_desconectado_alerta` aprovado no Meta Business Manager** (mesmo esquema dos outros dois alertas) antes de funcionar de fato em produção — sem o template aprovado, o canal WhatsApp do alerta falha silenciosamente (só o push continua funcionando).

**24/08/2026 (Claude, TASK-0029/0030/0033 + Manus.Ai em paralelo):**
- **Catálogo público da Monique — dois bugs reais de renderização encontrados e corrigidos após o dono do produto testar em produção pelo celular:** (1) a migração da landing do Netlify não trouxe a fonte serifada real (Playfair Display) — caía no fallback Georgia normal, que num título de até 94px fica fino e "fantasma"; corrigido importando a fonte certa (identificada por inspeção visual do print de referência, já que o site na Netlify não tem repositório/zip de origem — só um deploy manual "drop") e deixando os títulos em bold itálico. (2) mesmo com a fonte certa, os títulos continuavam ilegíveis — causa real: todo elemento com `color` explícito no CSS sempre renderizou certo, só os que dependiam de herança do `body` (h1/h2/h3/h4/`.brand`) ficavam lavados, provavelmente pelo "Tema escuro automático" do Chrome Android reagindo diferente a texto sem cor própria, já que a página compartilha o documento com o `index.css` do painel (que é escuro de propósito). Corrigido com cor explícita em todo título + `<meta name="color-scheme" content="light">` injetado via JS enquanto a página pública está montada. **Confirmado funcionando pelo dono do produto depois do segundo fix.**
- **Mensagem do WhatsApp do catálogo vira editável:** antes era texto fixo em `PublicCatalogPage.tsx`; agora a aba Catálogo tem dois campos (mensagem geral do botão flutuante + template por produto com `{produto}`), migration `0046_public_catalog_whatsapp_message.sql` aplicada em produção, colunas nullable com fallback pro texto padrão de sempre.
- **Em paralelo, Manus.Ai fez uma leva de UX/operação** (registros completos em `docs/task-registry/TASK-0025.md`, `TASK-0028.md`, `TASK-0031.md`, `TASK-0032.md`, PR #384): Agenda & Financeiro ganhou resumo do período, filtros (tipo/status/data), edição explícita de status e confirmação antes de excluir lançamento (TASK-0025); Central de Atendimento foi atomizada num shell mobile mais compacto com fila filtrável (novo filtro "Esperando você") e resumo operacional de 4 sinais (TASK-0028), com um bug real de override CSS tardio no Android corrigido na sequência (TASK-0031); a cópia de Base de Conhecimento de outro tenant foi agrupada dentro do painel recolhível de Modelos de Negócio (TASK-0032); e uma central supervisionada de anúncios Meta foi adicionada (PR #384). Nenhuma dessas mudanças tocou dado real, endpoint sensível ou isolamento multi-tenant.
- **Estado do repositório confirmado limpo:** `npm run lint`, `npx vitest run` (165 arquivos, 858 testes) e `npm run build` passam sem erro no `main` em 24/08. Nenhum PR aberto pendente, nenhuma issue nova desde 22/08. A issue #290 continua fechada sem sucessora (achado registrado em 23/08, ainda sem decisão do dono do produto sobre reabrir ou criar um painel novo).

**23/08/2026 (TASK-0026):**
- **Bug real corrigido (dado, não código):** a migration `0042_public_catalog.sql` (TASK-0010) foi aplicada em produção, mas o opt-in/contato público da Monique tinha sido gravado via SQL direto no tenant **errado** (`11111111-...`, que virou "Clic Piscinas (Meta)" no rename de 18/08) em vez do tenant real (`8a786c2a-...`, slug `monique-teste`) — `/catalogo/monique` mostraria a casca da Monique com os 21 produtos de piscina da Clic por baixo. Corrigido direto no Supabase; validado por query pós-fix.
- **Fix estrutural:** nova aba **Catálogo** no painel (`PublicCatalogSettings.tsx`, papel `admin`+) pra editar opt-in + contato (WhatsApp/Instagram/Maps/endereço/horário) sem SQL direto, com contador de produtos ativos linkado à Base de Conhecimento. Nova rota `GET/PUT /api/public-catalog-settings`, escopada só por `tenantOf(req)` (nunca slug/id do cliente) — elimina essa classe de erro daqui pra frente.
- **Achado de processo:** a issue #290 (painel canônico de status) foi encontrada **fechada** no início desta sessão, aparentemente sem querer (a própria issue diz para nunca usar `Closes #290`) — sem issue sucessora identificada. Registro desta tarefa ficou em `docs/task-registry/TASK-0026.md` e no corpo do PR.
- **Achado separado, corrigido na sequência (TASK-0027):** `npm run lint` (`tsc --noEmit`) estava quebrado no `main` desde a migração pro Express 5 (commit `63e8218`, PR #383) — 98 erros `string | string[]` em várias rotas. Causa raiz: `ParamsDictionary` (via `@types/express-serve-static-core`) alargou o tipo de `req.params` pra suportar segmentos repetidos que este projeto não usa em nenhuma rota. Corrigido num arquivo só (`server/middleware/auth.ts`, tipo `AuthenticatedRequest`) em vez de cast em cada um dos ~50 pontos de uso — `npm run lint` volta a passar limpo.

**20/08/2026:**
- Fallback determinístico anti-alucinação de horário (`autoReply.ts`) reduzido de 6 pra 3 horários e reformulado — o gate em si (Epic 4.5.7) não mudou, só a frase que ele usa quando dispara.
- **Bug real corrigido:** `reminderJob.ts` mandava o lembrete de agendamento sempre em português, mesmo pra tenants/leads de língua espanhola (a única tenant real hoje é paraguaia) — novo campo `tenants.reminder_language` (migration 0038, default `'es'`) resolve o texto certo por tenant.
- **Bug real corrigido:** `reminderJob.ts` disparava o lembrete "mesmo_dia" assim que a DATA batia com hoje, sem checar a HORA — um agendamento de hoje gerava "Bom dia!" às 00:30. Agora só manda depois do horário de abertura configurado do tenant (fallback 07:00 sem expediente configurado).
- Widget de agenda (`UpcomingEventsPanel.tsx`) ganhou: cor do dia por ocupação (verde/âmbar/vermelho), atalho "+" pra criar agendamento já com a data do dia clicado pré-preenchida, e **remarcar/excluir agendamento direto no painel** — antes só dava pra editar o título do serviço ou marcar como concluído, não existia NENHUM jeito de remarcar horário ou cancelar pelo painel (só recriando/apagando manualmente no Google Calendar). Sem drag-and-drop (avaliado, mas maior risco/esforço pro ganho — ficou só o form inline com data/hora).
- Cadastro manual de agendamento (`ManualAppointmentModal.tsx`) agora mostra só os horários REALMENTE livres (nova rota `GET /api/google-calendar/free-slots`, reaproveitando a mesma lógica de `findWeeklyAvailability`) em vez do operador digitar hora às cegas; e ganhou um campo opcional de "valor recebido" pra quando a cliente transfere um valor diferente do preço do catálogo (`paymentAmountReceived` em `POST .../manual-appointment`, propagado pro registro financeiro automático).
- **Bug real CONFIRMADO (validado ponta a ponta, não só suspeita):** o lembrete de agendamento com botões (`sendWhatsAppInteractiveButtons` em `reminderJob.ts`) usa mensagem livre, não template — só funciona dentro da janela de 24h desde a última mensagem do cliente (regra da própria Meta). Teste real feito ao vivo: enviado pra um número de controle, a Meta devolveu 200 na hora (por isso o log sempre diz "✅ Enviado"), mas ~4min depois chegou um webhook de status `"failed"` (código 131047, "Re-engagement message... more than 24 hours have passed since the customer last replied") — e a mensagem de fato **nunca apareceu no celular** (confirmado pelo dono do produto). `reminderJob.ts` não lê esse webhook de status nenhum, então essa falha é 100% silenciosa hoje — nem operador nem cliente sabem que o lembrete não chegou. `sendWhatsAppInteractiveButtons` agora loga o `wamid` (commit anterior) especificamente pra permitir esse tipo de investigação.
  - **Fix real exige template aprovado** (com botão quick-reply) no lugar da mensagem livre atual — só template funciona fora da janela de 24h. Precisa: (1) dono do produto criar e submeter o template no Meta Business Manager (corpo com `{{1}}` pro horário + 2 botões fixos "Confirmar"/"Reprogramar"), (2) código: `sendWhatsAppTemplateMessage` (`metaSend.ts`) hoje só manda texto, sem suporte a componente de botão — precisa ser estendido; `reminderJob.ts` troca pra essa rota.
  - **Isso NÃO bloqueia mais o item "lista interativa no fluxo de agendamento da IA"** (distinto do lembrete): esse fluxo acontece durante uma conversa em que o cliente acabou de mandar mensagem, então está naturalmente dentro da janela de 24h — não precisa de template, pode usar botão/lista livre como o lembrete tentava fazer.
- **Implementado:** o fallback determinístico anti-alucinação (`autoReply.ts`, oferece horários confirmados quando o modelo cita um horário não confirmado) agora também preenche `AutoReplyResult.quickReplyOptions` (bodyText + até 3 botões, um por horário — já batia com o limite de 3 da própria Meta). `sendBubbles.ts` manda a ÚLTIMA bolha como `interactive/button` de verdade no canal Meta (Evolution/Instagram continuam com o texto puro, sem suporte a esse tipo de mensagem). Quando o cliente toca no botão, `webhookParsers.ts` já tratava isso como texto normal (mesmo pipeline usado pelos botões de lembrete) — nenhuma mudança necessária do lado de entrada. Só cobre o caso do fallback determinístico por enquanto (não o texto livre "normal" do modelo, que não é estruturado o bastante pra virar botão com segurança).

**24/08/2026:**
- **Bug do item acima CORRIGIDO no código (TASK-0057)**, mas ainda **bloqueado em produção** até aprovação manual dos templates na Meta: `sendWhatsAppTemplateMessage` (`metaSend.ts`) ganhou suporte a componente `button`/`quick_reply` (parâmetro opcional `buttonPayloads`); `reminderJob.ts` no canal Meta agora manda via template aprovado (`lembrete_agendamento_es` / `lembrete_agendamento_pt`, por `reminder_language` do tenant) com botões `lembrete_confirmar`/`lembrete_remarcar`, em vez do texto livre com botão interativo que falhava fora da janela de 24h. `webhookParsers.ts` ganhou o branch pro toque nesse botão (`msg.type === 'button'`, formato diferente do `interactive.button_reply` de antes) — sem isso o toque cairia silenciosamente em `other`. Canal Evolution não muda (continua texto livre, Baileys não tem o conceito de template da Meta).
  - **Pendente, fora do alcance de qualquer sessão de código:** os 2 templates (`lembrete_agendamento_es` em espanhol, `lembrete_agendamento_pt` em português) ainda precisam ser criados e aprovados manualmente no Meta Business Manager. Até a aprovação sair, a chamada em produção vai falhar com erro de "template não encontrado/não aprovado" — o lembrete pelo canal Meta fica de fato inoperante nesse meio-tempo (o log de erro fica visível, diferente do silêncio do bug anterior). Texto de submissão exato entregue ao dono do produto fora deste repositório (chat da sessão que abriu TASK-0057).

**25/08/2026:**
- **Pesquisa comparativa (pedido direto de chat):** benchmark do fluxo do agente contra dois projetos open source do mesmo domínio (recepcionista de WhatsApp com agendamento real no Google Calendar, multi-tenant — `Hiberius/whatsapp-receptionist` e `martin-minghetti/whatsapp-ai-receptionist`) e frameworks de maturidade de agentes de IA (Microsoft L100-L400). Achados principais: (1) o gate de pagamento do Universo (`awaiting_payment→pending_verification→verified/rejected→confirmed`, IA nunca decide sozinha) é mais rígido que os dois comparáveis; (2) o mesmo gap de isolamento multi-tenant (RLS existe mas não é a linha de defesa real, isolamento é disciplina de código) aparece no par mais maduro também — não é descuido único do Universo; (3) anti-alucinação de horário do Universo era **reativa** (gera resposta livre, corrige depois), enquanto o padrão mais maduro observado é **preventivo** (nunca deixa o modelo calcular data livre); (4) Base de Conhecimento sem RAG/retrieval real (concatena tudo, já registrado como item #96) e observabilidade rasa (sem tracing por chamada, redação de log estreita) seguem como as maiores lacunas restantes.
- **TASK-0066 (item 1 da pesquisa, aplicado no mesmo dia):** `autoReply.ts` ganhou uma camada preventiva de anti-alucinação de horário — quando uma ferramenta de agenda confirma horário(s) de verdade nesta mensagem, o especialista recebe uma restrição imperativa explícita ("RESTRIÇÃO OBRIGATÓRIA DE HORÁRIO...") no próprio contexto, ANTES de gerar a resposta, em vez de só a frase narrativa solta que já existia. O gate reativo (Epic 4.5.7) não foi alterado — continua como rede de segurança. Escopo restrito a quando uma ferramenta rodou de verdade nesta mensagem (nunca só por existir um agendamento antigo), pra não atrapalhar um "quero remarcar" sem novo horário ainda. Sem telemetria hoje pra medir se a taxa real de disparo do gate reativo cai em produção (mesmo gap de observabilidade apontado na pesquisa).
- **Incidente de cota do Supabase (TASK-0074/0075) + gestão de tenants (TASK-0072) + fix de safe-area no iPhone (TASK-0077):** ver linha "Custo de infraestrutura (egress do Supabase)" na tabela acima e o registro de cada TASK em `docs/task-registry/`. Todo esse trabalho está na branch `claude/multitenancy-security-privacy-ths4pn`, ainda sem PR aberto — acumula 4 tarefas aguardando autorização pra abrir PR/merge por tocarem área sensível (auth, isolamento de tenant). O fix de safe-area (header colado na barra de status do iPhone em PWA) está pendente de confirmação visual do usuário num iPhone real.

**03/09/2026:**
- **TASK-0243/TASK-0249 — Achado de segurança: `localStorage` sem criptografia é padrão sistêmico no painel.** Corrigindo um bloqueio real de CI (CodeQL, 2 alertas HIGH "Clear text storage of sensitive information") no PR #625, encontrado: `WhatsAppLeadsSim.tsx` gravava o array `leads` inteiro — nome, telefone, histórico completo de mensagens de clientes reais, incluindo mídia base64 de comprovantes — em `localStorage` sem nenhuma criptografia (`saas_whatsapp_leads_${tenantId}`). Confirmado que esse cache só existia por performance (pintura instantânea antes do fetch real responder), sem nenhum comportamento funcional dependendo dele — corrigido eliminando a gravação/leitura por completo (TASK-0243), com purga ativa das chaves já gravadas em navegadores de sessões anteriores. **Achado colateral mais sério, ainda não corrigido (TASK-0249):** o mesmo padrão existe pra `saas_auth_token` (o JWT de sessão do operador — mais grave que PII, uma leitura da chave rouba a sessão inteira), `meta_capi_config` (access token real da Meta Conversions API), e os caches de CRM/financeiro/base de conhecimento em `App.tsx`. Nenhum helper de criptografia/ofuscação existe em `src/lib/` hoje. Sem correção ainda — aguardando o dono do produto decidir prioridade/abordagem por item (ver `docs/task-registry/TASK-0249.md` pro detalhe completo e a recomendação de por onde começar).
- **Central de Operação por WhatsApp (TASK-0220) — feature entregue por outra sessão do Claude Code local (Windows), auditada e corrigida antes do merge.** A sessão local reportou "concluído" (lint/testes/build passando) só com commits locais, sem push nem PR — sem verificação independente não dava pra confiar. Depois de guiar o usuário até o push (`feat/central-operacao-whatsapp`), a auditoria encontrou um problema real e sério: `GET /api/conversations/:phone/templates` (reabertura de conversa fora da janela de 24h da Meta) devolvia 4 templates **inventados**, iguais pra qualquer tenant, e o frontend tinha o nome do negócio hardcoded como `"Universo Estética"` — vazaria pra clientes reais de qualquer outro tenant, e a Meta rejeitaria o envio de qualquer forma (templates fictícios nunca existiram numa conta real). Corrigido: busca de verdade na conta WhatsApp Business (WABA) do tenant via Graph API (`listApprovedMetaMessageTemplates` em `metaSend.ts` + `resolveMetaTemplateCredentialsForTenant` em `tenantResolver.ts`, sem fallback pra credencial compartilhada — WABA é por tenant), nome do negócio vindo do tenant real, e a página agora bloqueia "Reabrir a conversa" diretamente (sem abrir o modal) quando o tenant não tem WABA configurado. Achados menores corrigidos na mesma revisão: duração/progresso de áudio fabricados na bolha de mensagem (`ConversationMessageBubble.tsx`, mostrava "0:15" fixo pra qualquer áudio) trocados por dado real do `<audio>`; componente `ConversationComposerBar.tsx` removido por estar órfão (nunca importado). Validação rodada pela sessão que auditou, não só a que reportou: `npm run lint` / `npx vitest run` (251 arquivos, 1376 testes) / `npm run build` limpos após o merge com a `main`. Ver `docs/task-registry/TASK-0220.md` pra detalhe completo.
- **TASK-0252 — Disparo em Massa vira módulo com liga/desliga por tenant.** O broadcast/marketing (TASK-0171) estava hard-gated a `saas_admin` em todas as ~22 rotas de `server/routes/broadcast.ts` e só existia dentro do SaaS Admin Dashboard — nenhum tenant normal conseguia usar. Virou um módulo opt-in de verdade, reaproveitando ponta a ponta o padrão já em produção de Logs do Sistema (`operations.system_logs`): nova feature `marketing.broadcast` nasce fechada pra todo mundo (`default_enabled=false`, migration `0078_broadcast_feature_gate.sql`), o SaaS Admin libera por tenant via `TenantEntitlementsModal.tsx` já existente (sem UI nova). Papel mínimo do lado do tenant: `admin` (rota rebaixada de `requireRole('saas_admin')` pra `requireRole('admin')`). Aparece no menu "Configurar" (`Header.tsx`) e renderiza em `App.tsx` só quando a feature está liberada — mesmo padrão exato de `canSeeSystemLogs`. Corrigido no mesmo PR um bug real de responsividade reportado com print (mobile): 3 linhas de formulário em `BroadcastAdminPanel.tsx` (`:767`/`:780`/`:983`) tinham `flex items-end gap-N` sem `flex-wrap`, com um campo `flex-1` que encolhia até ~1 caractere visível em vez do layout quebrar linha — corrigido com `flex-wrap` + `min-w-[160px]`. Ver `docs/task-registry/TASK-0252.md`.
- **TASK-0258 — Tela de Atendimento desafogada (pedido direto com print real, mobile): 4 cards de aviso empilhados viram sinal compacto.** 3ª/4ª rodada de enxugamento dessa mesma tela (depois de TASK-0185/0187). `src/components/WhatsAppLeadsSim.tsx` (mais `ContactContextPanel.tsx`): (1) sugestões de etiqueta saem da barra sempre visível e passam a morar dentro do popover do botão "+ Nova", só aparecem sob demanda; (2) faixa de status da janela de 24h — quando aberta e sem ação pendente, vira badge pequeno no cabeçalho (mesmo padrão visual do pill "ao vivo"), em vez de card de linha inteira; o card cheio com botão continua igual a hoje quando a janela fecha (aí sim há ação real); (3) aviso fixo "Ao enviar, você assume esta conversa..." deixa de ser parágrafo permanente e vira ícone `Info` com tooltip + alert no toque; (4) texto do card "CONTEXTO SUPERVISIONADO" trocou `truncate` (cortava no meio da palavra) por `line-clamp-2`. Alerta de escalonamento (sinal de segurança, não decoração — decisão já registrada em TASK-0187) continua intocado, sem botão de fechar. Nenhuma mudança de backend/lógica, só apresentação. Ver `docs/task-registry/TASK-0258.md`.
- **TASK-0259 — 2ª rodada de enxugamento do Atendimento, mesma sessão logo após a TASK-0258.** Feedback direto do usuário sobre o próprio resultado anterior: (1) o ícone de aviso de assunção de controle (recém-adicionado na TASK-0258) virou um banner discreto que aparece só na 1ª vez que o operador toca no campo de digitação (flag `saas_composer_takeover_hint_seen` em localStorage) — campo de texto volta a ter só emoji/texto/anexo dentro da caixa e enviar/mic fora, como era antes; (2) achado técnico real: o contorno verde de foco (`:focus-visible`) no campo de digitação não sumia com `focus:outline-none` do Tailwind porque no Tailwind v4 (CSS-first) utilitários vivem em `@layer utilities`, e CSS solto fora de qualquer `@layer` (como a regra global em `index.css`) sempre vence camada nomeada nas Cascade Layers, independente de especificidade — corrigido com uma regra específica só pro campo da conversa, sem tocar a acessibilidade do resto do app; (3) cards de "CONTEXTO SUPERVISIONADO" e escalonamento com padding/fonte reduzidos, escalonamento voltou de rose pra âmbar/laranja ("como era antes"); (4) telefone saiu do cabeçalho da conversa, fica só na Ficha do Contato; (5) a barra de etiquetas sempre visível foi eliminada por completo — o ícone de etiqueta subiu pro cabeçalho ao lado do botão da Ficha IA, com ponto verde indicando etiqueta aplicada, popover mostra etiquetas aplicadas (removíveis) + sugestões + criar nova, mesma lógica de antes. `src/components/WhatsAppLeadsSim.tsx` + `src/index.css`, nenhuma mudança de backend. Ver `docs/task-registry/TASK-0259.md`.
- **TASK-0263 — Popup "Agenda" (eventos reais do Google Calendar) ganha tela cheia no mobile, botões deixam de ficar escondidos, desconectar vai pra aba Agenda de verdade.** Pedido direto com print real. Achado técnico: os botões de pagamento/editar/remarcar/excluir de cada agendamento (`UpcomingEventsPanel.tsx`) usavam `opacity-0 group-hover:opacity-100` — invisíveis no toque (sem hover real), não clipping por largura. Corrigido: sempre visíveis. O popup também ganhou tela cheia no mobile (`w-full h-full`, sem `max-w`/`max-h`/cantos arredondados) — a partir do breakpoint `sm:` continua como modal centralizado, mesmo padrão dos outros modais simples do painel. O botão "Desconectar Google Calendar" (que já tinha morado em Ferramentas, depois dentro deste popup) saiu de novo — a aba "Agenda" real do menu principal (`AgendaWorkspace.tsx`) é uma tela separada (financeiro/pagamentos, sem nenhuma ligação com o Google Calendar até aqui); o botão agora mora lá, dentro de um componente novo e autocontido (`GoogleCalendarConnectionControl.tsx`, busca o próprio status, não depende de state do Atendimento). Nenhuma rota de backend nova — `/status`/`/connect`/`/disconnect` já existiam, só o componente que os chama mudou de lugar. Ver `docs/task-registry/TASK-0263.md`.
- **TASK-0289 — Menu mobile do Header vira gaveta overlay de verdade; botão de tema deixa de ser binário quebrado; "Notificações Push" some como caixa própria.** Pedido direto com print. Achado de sincronização importante: a sessão estava ~31 commits atrasada da `main` (outra sessão paralela mesclou TASK-0274 a 0288 nas horas anteriores, incluindo a TASK-0284 que moveu "Notificações Push" pra dentro do `Header.tsx`, removendo do menu ⋮ da conversa) — confirmado direto no Render (`list_deploys`) antes de continuar, evitando "corrigir" uma duplicação que não existia mais. Achados reais: (1) o ícone de tema (mobile e desktop) ciclava nos 4 temas mas o ícone/tooltip só tinha 2 estados (Sun/Moon) — pra light/blue/clean (3 dos 4) mostrava a mesma lua e a mesma dica "Modo escuro"; corrigido com um popover que mostra o tema atual corretamente e lista os 4 nomeados com checkmark, reaproveitando a mesma infraestrutura de menu suspenso já usada por "Configurar" no mesmo arquivo; (2) fileira "Modo visual" (separada, 4 botões) eliminada — função absorvida pelo popover; (3) o "espaço vazio" reportado não era CSS entre elementos (não existe) — era o menu mobile sendo conteúdo INLINE que empurrava a lista de conversas pra baixo em vez de flutuar por cima; virou `absolute` ancorado no fim do `<header>` (que já é `sticky`, serve de bloco de contenção sem precisar calcular altura em pixel), com o véu de fundo ganhando opacidade (antes 100% transparente) — agora abre "como uma gaveta" (pedido literal do usuário), sem empurrar nada; (4) "Notificações Push" perde a caixa própria e vira sub-linha discreta dentro do card "Empresa ativa" — continua funcional, só menos destacada. Só `src/components/Header.tsx`, nenhuma mudança de backend/lógica. Ver `docs/task-registry/TASK-0289.md`.
- **TASK-0299 — 2º round sobre a TASK-0289 + ajustes na conversa, pedido direto com 3 prints.** (1) O overlay `absolute top-full` da TASK-0289 ainda tinha altura baseada no CONTEÚDO, não na tela — sobrava espaço "morto" acima/abaixo do cartão. Virou `fixed inset-y-0 right-0` (gaveta lateral direita de largura fixa, `w-[82vw] max-w-xs`, ocupando a altura REAL da tela — `position: sticky` não é bloco de contenção pra `fixed`, então o menu se solta do header e cobre a tela inteira por definição, eliminando o espaço morto), reaproveitando `animate-pop-in` (mesma classe do menu ⋮ da conversa, pedido explícito do usuário) com `origin-right`; gesto de fechar virou "arrastar pra direita" (era "pra cima"). (2) Menu ⋮ da conversa (`WhatsAppLeadsSim.tsx`): removidos os 4 divisores `border-t border-slate-700` entre grupos de ações — liam como linhas azuladas no tema escuro ("margens azuis" no print). (3) Painel de anexos (`WhatsAppLeadsSim.tsx`): cartão com borda/cantos arredondados e margem visível virou `-mx-2` (cancela o `p-2` do wrapper pai) sem borda — rente às laterais da tela; "Foto de exemplo"/"Vídeo de exemplo" tinham 1 ícone POR PRODUTO cadastrado (sem teto) — viram 1 ícone só por tipo de mídia, expandindo a lista de produtos abaixo da grade sob demanda. Ver `docs/task-registry/TASK-0299.md`.
- **TASK-0306 — "Horários de atendimento" nunca refletia na tela, mesmo já salvo no banco.** Print real: modal com todos os 7 dias desmarcados, "já fizemos várias tentativas mas os horários ainda não persistem". Investigação (não confiei no relato à primeira vista): consulta direta ao Supabase mostrou que `tenants.business_hours` já estava correto (seg-sáb 09:00-18:00) — a gravação nunca foi o problema (o fix de RLS da TASK-0187, `getPlatformDb()` no `setTenantBusinessHours`, segue válido). Causa raiz real, em `App.tsx`: o `useEffect` que busca `/api/business-hours` tinha `useEffect(..., [])` — sem checar `currentUser`, dispara uma única vez no mount, com o token/`X-Tenant-Id` que existir NAQUELE instante, e nunca tenta de novo pelo resto da sessão se rodar antes da sessão/tenant terminarem de resolver. Mesma classe de bug já encontrada e corrigida 3 vezes nesse mesmo arquivo (efeitos de `activeTab`/`tenantCapabilities`/`activeTenant`) — só a busca de horários ainda não tinha o guard `if (!currentUser) return;` + deps reativas (`currentUser?.id`, `activeTenant.id`). Corrigido nesse padrão; também passa a resetar pra `{}` quando o backend não devolve nada (evita mostrar o horário do tenant anterior numa troca de empresa pelo saas_admin). Ver `docs/task-registry/TASK-0306.md`.
- **TASK-0307 — Achado de arquitetura: dois sistemas de Base de Conhecimento em paralelo, fora de sincronia (não é um bug pontual).** Usuário editou e publicou a correção do campo "Link de Localização" (mesma poluição de texto da TASK anterior), mas o botão "Localização" do menu de anexos continuou mandando o texto antigo pro cliente 15min depois. Achado: `knowledge_base_documents` (sistema novo, documentos tipados com draft/publish, ISSUE-0096/PR2 — o que `AgentKnowledgeBase.tsx` edita e o que `getRuntimeKnowledgeBase()`/`autoReply.ts` leem pra gerar as respostas da IA) tinha o valor correto; `knowledge_base` (sistema antigo, 1 linha jsonb por tenant, "legacy blob" — o que `GET/POST /api/knowledge-base` lê/grava DIRETO, alimentando `App.tsx`/os botões manuais da conversa E ainda usado por ~10 outros call sites do backend via `getKnowledgeBase()` sem "Runtime" no nome: `webhooks.ts`, várias rotas de `conversations.ts`, `transcriptionQueue.ts`) continuava com o texto poluído — **publicar no sistema novo nunca escreve de volta no antigo**. Achado colateral mais sério no mesmo diff: `agentGoal` (instrução-mestra da IA) estava em ESPANHOL no blob antigo vs. PORTUGUÊS atualizado no publicado; `paymentDetailsText` do blob antigo estava `null` (botão "Dados da conta" nem aparecia). Ação imediata: sincronizei manualmente os 5 campos de `business_profile` do documento publicado pro blob antigo via `UPDATE` direto no Supabase, só pro tenant Monique - Evolution — hotfix de DADO, não de código, e não geral (qualquer outro tenant com a mesma divergência continua afetado; uma nova publicação futura volta a divergir). Fix de código recomendado, NÃO implementado (a rota `GET /api/knowledge-base` teria que trocar a leitura crua por `getRuntimeKnowledgeBase()`, o que exige recalcular o ETag hoje baseado em `updated_at` do blob — usado num teste real de um incidente de egress ~12MB — e decidir se a migração dos ~10 call sites legados é a tal "corte de runtime aprovado em PR4" que o próprio código já menciona como pendente). Ver `docs/task-registry/TASK-0307.md`.

## Protocolo de sincronização entre agentes

A issue [#504](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/504) é o painel canônico de status e handoff entre Manus.Ai, Claude e outras plataformas (v2, rotada da [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290) em 28/08/2026 — #290 fica fechada só como histórico read-only, nunca reaberta pra novo registro). Toda nova conversa de desenvolvimento deve consultar a #504 antes de analisar, editar código, alterar configuração ou escolher uma tarefa. Para gerar um identificador sequencial compartilhado, execute `npm run task:start -- --agent "NomeDaPlataforma" "descrição da tarefa"`; o comando usa um lock local contra colisões, lê o maior `TASK-XXXX` já versionado e cria o próximo registro em `docs/task-registry/`. Ao concluir, o agente deve atualizar esse registro e a #504 com o identificador, resultado, arquivos/commits, validações e pendências. Antes de iniciar outra tarefa, sincronize a `main` para que a sequência permaneça compartilhada pelo Git.

## Docs relacionados (contexto histórico/profundo, não confie neles pro status atual)

- `CLAUDE.md` (raiz) — convenções de código, comandos, e as regras de arquitetura que não mudam com frequência (async error handling, idempotência, multi-tenancy). **Ainda é a referência viva pra "como escrever código aqui"**, só não pra "o que já foi feito".
- `docs/AGENTE-VERTICAL-ARQUITETURA.md` — arquitetura do prompt em camadas do agente (global/tenant/dinâmico). Continua consistente com o código atual, é o doc certo pra entender o design do prompt em detalhe.
- `docs/AGENTE-PROMPT-MONIQUE-CAMPOS.md` — snapshot do conteúdo real do prompt/KB do tenant Monique. É conteúdo editável no painel, não código — sempre reconfirme no Supabase antes de operar em cima dele.
- `docs/PLANO-EVOLUCAO.md` — **histórico de planejamento, desatualizado desde ~06/08/2026.** Descreve "Fase 2 — multi-tenant" como pendente; a auditoria de 19/08 confirma que a maior parte já está feita (ver tabela acima). Mantido só como registro de decisões passadas — não use pra saber o que falta.
- `docs/REVISAO_E_REESTRUTURACAO.md` — **snapshot muito mais antigo, pré-refatoração**, descreve um estado do código (3 backends não integrados, login sem senha real, zero testes) que não existe mais. Mantido só como registro histórico de onde o projeto começou.
- `docs/ORGANIZACAO-COMERCIAL-SEGURANCA-PRIVACIDADE-TENANTS.md` (TASK-0070, 25/08/2026) — **proposta, não implementada**: estrutura comercial de tenant (ciclo de vida/plano, hoje inexistente) e privacidade de dados pessoais dos leads (inventário, consentimento/opt-out, retenção, resposta a incidente). Aprofunda o gap #4 (RLS não é a barreira real) dentro do enquadramento de privacidade. Nenhum item de lá virou código ainda — confira aqui antes de assumir algo como feito.
