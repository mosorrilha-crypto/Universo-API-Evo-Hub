# TASK-0080 — Fix contraste de placeholder no tema Azul/Escuro + hotfix de main quebrado (whatsappUrl variantes)

- **Iniciado em:** 2026-08-26T02:37:13.647Z
- **Agente/plataforma:** Claude
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** em andamento

## Objetivo

Fix contraste de placeholder no tema Azul/Escuro + hotfix de main quebrado (whatsappUrl variantes)

## Atualizações

Pedido direto de chat, com print do celular: no tema Azul, o campo "Descrição
desta variação" (editor de variantes do catálogo) mostrava o placeholder
quase ilegível — cinza padrão do Tailwind contra o fundo azul-marinho
escuro. Causa: `html[data-theme='blue']` em `src/index.css` só remapeava
`.placeholder-slate-400`, mas essa tela usa `placeholder-slate-500`/`-600`
(auditado: 27 ocorrências no app usam esses dois, contra só 2 de -400).
Estendida a mesma correção ao tema Escuro (`data-theme='dark'`), que tinha
exatamente o mesmo gap.

**Achado separado, corrigido no mesmo commit por bloquear a validação:**
antes de commitar, `npm run lint` estava quebrado no HEAD do `main`
(confirmado que já estava quebrado ANTES da minha mudança, com
`git stash` + lint) — `PublicCatalogPage.tsx` chamava `whatsappUrl()` com
4 argumentos numa das 3 chamadas (faltava `slug`, primeiro parâmetro),
provável artefato de merge entre duas PRs concorrentes recentes
(`#423`/commit "adiciona mídia e mensagem por variação") que mexeram nessa
função sem se sincronizar. Corrigido pra bater com o padrão das outras 2
chamadas no mesmo arquivo. Como bloqueava `npm run lint` pra QUALQUER PR
novo (não só o meu), tratado como hotfix urgente no mesmo commit.

Entregue: `src/index.css`, `src/components/PublicCatalogPage.tsx`.

## Encerramento

**Status: concluído.** Dado que o hotfix não toca autenticação/pagamento/
mensageria/isolamento multi-tenant e estava bloqueando `main` pra todo
mundo, farei self-merge assim que o CI confirmar verde (mesmo padrão já
usado antes no projeto, ex: PR #354). Commits/validação: ver comentário na
issue #290.

### Modelo de atualização na #290

```markdown
### TASK-0080 — Claude — 2026-08-26T02:37:13.647Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
