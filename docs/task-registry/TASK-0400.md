# TASK-0400 — Textos mais didáticos no painel de Notificações (explicações com exemplo concreto por item)

- **Iniciado em:** 2026-09-12T13:09:33.357Z
- **Agente/plataforma:** Claude
- **Issue central:** [#758](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/758) (v3 do painel — o script gerou o link antigo #504, ver CLAUDE.md pra rotação)
- **Status:** concluído

## Objetivo

Textos mais didáticos no painel de Notificações (explicações com exemplo concreto por item)

## Atualizações

Pedido direto (12/09/2026, mesmo dia da TASK-0399): "ficou bom o painel de notificações mas você pode deixar as orientações ainda mais didáticas pra os clientes entenderem melhor, como ensinar uma criança de 5 anos". Reescrita de copy pura, sem mudança de comportamento/lógica — cada item das duas telas do painel de Notificações ganhou um "Exemplo:" com uma situação concreta, em vez de só uma descrição técnica curta:

- **`src/components/AlertSettingsPanel.tsx`** (aba "Alertas internos"): os 5 itens (agente pausado, WhatsApp caiu, erro de sistema, escalonamento, pagamento pendente) agora trazem um cenário real ("Exemplo: você pausou a IA..."). Cabeçalho do número de telefone explica com analogia ("igual um lembrete de um amigo") que o alerta nunca é mensagem de cliente. Caixa de aviso final reforçada: "desmarcar um item aqui nunca faz você perder o aviso — só faz você não ganhar a mensagem extra no celular".
- **`src/components/CustomerNotificationSettingsPanel.tsx`** (aba "Mensagens automáticas pro cliente"): as 3 seções (lembrete de agendamento, retomada de conversa parada, reengajamento do funil) ganharam explicação de cenário + um parágrafo "O que isso significa:" abaixo de cada grupo de campos numéricos/horário, traduzindo o número pra linguagem simples (ex.: "72" → "um cliente que marcou 3 dias antes recebe o lembrete; um que marcou ontem pra amanhã, não"; "2.5" → "2 horas e meia"). A explicação da janela de 24h do WhatsApp na retomada também ficou mais concreta (por que a IA não pode simplesmente escrever livremente pro cliente depois de 24h).

**Arquivos/commits:** `src/components/AlertSettingsPanel.tsx`, `src/components/CustomerNotificationSettingsPanel.tsx`. Sem mudança em nenhum arquivo de backend, rota, migration ou lógica — só JSX/texto.

**Validação:** `npm run lint`, `npx vitest run` (303 arquivos / 1734 testes — nenhum teste depende desses textos, confirmado por grep antes de reescrever) e `npm run build` — todos limpos. **Sem validação visual** — mesma limitação já registrada nas duas tasks anteriores (TASK-0396/TASK-0399): sandbox sem credenciais reais pra subir o app e logar.

**Pendente ou risco:** nenhum risco de regressão (mudança só de texto/copy, sem alterar nenhum dado enviado à API nem nenhuma lógica de negócio). Recomendo o dono do produto ler as duas telas depois do deploy e confirmar se o tom/nível de detalhe ficou adequado — "didático" é subjetivo, pode precisar de um ajuste fino de linguagem depois de ver na tela real.

## Encerramento

Ao concluir, atualize o status, informe os commits e as validações na issue #504 usando o identificador **TASK-0400**.

### Modelo de atualização na #504

```markdown
### TASK-0400 — Claude — 2026-09-12T13:09:33.357Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
