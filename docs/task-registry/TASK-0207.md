# TASK-0207 — Adiciona mapa visual do projeto (HTML) gerado como artifact para consulta rápida de onboarding

- **Iniciado em:** 2026-09-02T00:13:52.669Z
- **Agente/plataforma:** Claude
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** concluída

## Objetivo

Adiciona mapa visual do projeto (HTML) gerado como artifact para consulta rápida de onboarding

## Contexto

O usuário pediu um "mapa explodido" visual do projeto (arquitetura, fluxos,
o que já está ativo) por ser iniciante e achar o repositório grande demais
pra se orientar só pelos docs em texto. Foi gerado como Artifact (HTML
autocontido com diagramas Mermaid do fluxo de mensagem e do fluxo do
painel, tabela de estado real por área e lacunas conhecidas, todos com
conteúdo extraído de `docs/GUIA-DO-PROJETO.md`) e compartilhado com o
usuário. Ele pediu em seguida para adicionar esse mesmo arquivo ao
repositório.

## Mudança

- `docs/mapa-do-projeto/index.html`: cópia do HTML publicado como Artifact
  — página autocontida (CSS + Mermaid via CDN), sem dependência do backend
  da aplicação. Não é servido por nenhuma rota; é pra abrir localmente no
  navegador.
- `docs/GUIA-DO-PROJETO.md`: nova seção "Mapa visual (onboarding)" logo no
  topo, apontando pro arquivo e deixando explícito que é um snapshot
  gerado a partir do próprio guia (2026-09-01/02) — se o guia mudar depois,
  o mapa pode ficar desatualizado até alguém regenerá-lo. O guia continua
  sendo a fonte de verdade, o mapa é só uma visualização derivada.

## Validação

- `npm run lint` (tsc --noEmit).
- `npx vitest run`.
- `npm run build`.
- Revisão manual: HTML é conteúdo estático, sem alteração de código de
  aplicação/rota/lógica de negócio.

## Pendente/risco

- O mapa é um snapshot, não gerado automaticamente a partir do guia —
  qualquer mudança futura no `GUIA-DO-PROJETO.md` (nova fase concluída,
  gap fechado) não atualiza o HTML sozinho; alguém precisa regenerá-lo
  manualmente quando o desalinhamento incomodar.

## Atualização (mesma tarefa, 2ª rodada)

O usuário revisou a primeira versão e apontou que faltavam muitos
processos/funcionalidades reais do produto, e pediu duas coisas: (1) cobrir
mais do ecossistema, não só o fluxo de mensagem; (2) representar as
pendências **dentro do próprio fluxo visual** (nó tracejado), não como uma
lista solta separada — e removeu a seção de Obsidian/skills numa rodada
anterior por poder confundir outros analistas.

Levantamento adicional em `server/routes/`, `server/services/` e
`src/components/` (muito além do que o guia resume em uma tabela) e
cruzamento com o changelog do `GUIA-DO-PROJETO.md` pra confirmar o que é
real vs. planejado. `docs/mapa-do-projeto/index.html` ganhou 4 diagramas
novos (Mermaid, mesmo padrão visual dos 2 já existentes):

- **Agenda & Financeiro**: pré-reserva → lock → hold expira ou confirma →
  lembrete (nó pendente: template do Meta não aprovado) → comprovante →
  confirmação humana → transação financeira → despesa recorrente (nó
  pendente: sem gateway de pagamento real).
- **CRM, Escalonamento & Qualidade**: funil de vendas → lead ganho vira
  cobrança automática; resposta arriscada → revisor pré-envio → rascunho
  bloqueado → "Aprovar e enviar" → vira exemplo aprovado que realimenta o
  agente; Central de Qualidade como evidência humana (nó pendente/parcial:
  não altera o agente sozinha, por decisão de escopo).
- **Marketing, Catálogo público & Oferta comercial**: Meta Ads + CAPI +
  Central de Tráfego; catálogo público por tenant; página `/oferta` de
  planos comerciais (nó pendente: ativação de piloto novo é manual);
  disparo em massa (nó pendente/parcial: sem botão liga/desliga manual).
- **Administração, Entitlements & Alertas de sistema**: criação de tenant →
  checklist de ativação → entitlements → RBAC; os 4 jobs de alerta
  (agente pausado, WhatsApp caiu, erro genérico, pagamento pendente) → nó
  pendente: sem error tracking geral fora desses 4 casos.

A tabela "Estado real por área" também foi ampliada (CRM, escalonamento,
qualidade, Meta Ads, catálogo público, oferta comercial, entitlements,
alertas de sistema) e **um erro de fato foi corrigido nela**: a linha do
CRM dizia "ainda é localStorage puro" (repetindo uma frase desatualizada
do `CLAUDE.md`) — mas o próprio `GUIA-DO-PROJETO.md` (que é a fonte que
vence em caso de conflito) já registra que `OperatorCRM.tsx` está ligado à
API real desde antes, com ponte pro Financeiro em leads ganhos. Corrigido
o chip de "lacuna" pra "ativo" nessa linha.

A seção "Lacunas conhecidas" (lista solta) foi removida — as pendências
agora vivem só dentro dos fluxos, como pedido.

### Validação (2ª rodada)

- `npm run lint` — limpo.
- `npx vitest run` — 237 arquivos / 1275 testes, todos passando (só
  conteúdo HTML/Markdown mudou, nenhum código de aplicação).
- `npm run build` — limpo.

### Pendente/risco (2ª rodada)

- Mesmo risco de antes: é um snapshot manual, desalinha do guia com o
  tempo se ninguém regenerar.
- `CLAUDE.md` (raiz) continua com a frase desatualizada sobre
  `OperatorCRM.tsx` ser mock puro — não foi corrigida nesta tarefa por
  estar fora do escopo pedido (só o mapa visual); registrando aqui pra
  quem for atualizar `CLAUDE.md` depois.

## Encerramento

Ao concluir, atualize o status, informe os commits e as validações na issue #290 usando o identificador **TASK-0207**.

### Modelo de atualização na #290

```markdown
### TASK-0207 — Claude — 2026-09-02T00:13:52.669Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```

## Atualização (mesma tarefa, 3ª rodada)

Dois pedidos novos do usuário:

1. Adicionar ao mapa "o que cada arquivo do repositório representa".
2. O PR #575 acabou de ser mesclado ao `main` e contém um sistema de
   avaliação/teste automático do agente — refletir isso no mapa.

### PR #575 (mesclado 02/09/2026, `f84b2cf`)

A branch de trabalho (`claude/gestor-refinamento-atendimento-mwu1a7`, que
tinha sido reiniciada do `main` na 1ª rodada desta tarefa) estava atrás do
`main` outra vez porque o #575 mesclou durante esta sessão — trouxe via
`git merge origin/main` (merge commit, sem rebase, pra não reescrever
histórico) em vez de assumir que já estava incluído. Trouxe também
TASK-0180 (cancelada/duplicada), TASK-0193 (anti-repetição no prompt da
Ficha IA), TASK-0196 (severidade de incidente do Gemini) e TASK-0203 (o
sistema de avaliação em si: `server/services/agentEvalService.ts` +
`scripts/eval-agent.ts`, `npm run eval:agent`).

Adicionado ao fluxo 03 (CRM/Escalonamento/Qualidade) um terceiro subgrafo
"Avaliação automática do agente": gera perguntas sintéticas com Gemini a
partir da KB real → roda no pipeline de produção de verdade
(`generateAutoReplyForText`, sem tocar agenda/WhatsApp) → passa pelo mesmo
revisor de segurança + um novo juiz de qualidade → detecta frase repetida
entre casos → cai na Central de Qualidade como evidência (nunca corrige o
agente sozinho, mesmo padrão do resto do projeto). Nova linha também na
tabela "Estado real por área".

### Mapa de arquivos (nova seção 06)

`docs/mapa-do-projeto/index.html` ganhou a seção "O que cada arquivo do
repositório representa", com grupos recolhíveis (`<details>`) cobrindo:
raiz/configuração, as 19 rotas (`server/routes/`), ~95 arquivos de
`server/services/` agrupados por assunto (mensageria/agente, canais/mídia,
agenda, financeiro, CRM/qualidade, base de conhecimento/marketing/
catálogo, multi-tenant/admin/infraestrutura), middleware + scripts, e os
principais componentes de `src/`. Não é 100% exaustivo (arquivos de teste
`*.test.ts` e as 74+ migrations SQL foram resumidos como grupo, não
listados um a um) — é o glossário do que tem lógica de negócio real.

### Validação (3ª rodada)

- `npm run lint` — limpo.
- `npx vitest run` — 238 arquivos / 1288 testes (já inclui os testes
  trazidos pelo merge do #575).
- `npm run build` — limpo.

### Pendente/risco (3ª rodada)

- O glossário de arquivos é manual, como o resto do mapa — arquivo novo
  criado depois desta tarefa não aparece sozinho.
- `npm run eval:agent` ainda não tem botão no painel (registrado como
  próximo passo no próprio PR #575, não implementado); o mapa já deixa
  isso explícito.

## Atualização (mesma tarefa, 4ª rodada)

Usuário afirmou "o botão já está em produção" (referindo-se ao botão de
disparar a avaliação automática do agente sem terminal, registrado como
pendência na 3ª rodada). Antes de mudar o mapa, auditei em vez de confiar
(regra do CLAUDE.md): `git log --oneline origin/main` mostrava só até o
commit do #575, nenhuma rota/componente de eval-agent tinha sido adicionado
em outro PR mesclado, e o único candidato era o PR #587 — mas estava
`draft`/`open`, não mesclado ainda. Reportei a discrepância ao usuário em
vez de atualizar o mapa como se já fosse real, expliquei o que o #587
continha e perguntei como proceder; ele optou por esperar a outra sessão
terminar (agendei um `send_later` de 45min pra checar sozinho).

O usuário confirmou o merge antes do check-in agendado ("pronto foi
mesclado"). Reconfirmei via `pull_request_read` (`merged: true`,
`merged_at: 2026-09-02T01:25:19Z`) antes de tocar no mapa — o PR na
verdade saiu como #587 "TASK-0208/0209" (2 tasks, não só a do botão):

- **TASK-0208** — o botão em si: `POST/GET /api/quality-audit/eval-runs`
  (`agentEvalRunStore.ts`, migration `0075_agent_eval_runs.sql`, RLS por
  tenant), execução em background (nunca `await` na rota, erro sempre
  capturado), card novo em `QualityAuditCenter.tsx` com polling de 3s.
  `scripts/eval-agent.ts` virou wrapper fino de `runAgentEvaluation`.
- **TASK-0209** — achado crítico não relacionado ao botão, mas real e
  grave: `transcriptionQueue.ts` cortava o histórico do agente por
  POSIÇÃO (assumindo que o áudio era sempre a última mensagem), e se o
  cliente mandasse uma mensagem nova enquanto o áudio ainda transcrevia,
  essa mensagem nova era descartada do contexto — mesma classe de bug de
  uma correção anterior (TASK-0172). Corrigido pra cortar por identidade
  do próprio áudio. `CLAUDE.md` também foi corrigido nesse PR: a
  idempotência de webhook não é mais "Set em memória" (desatualizado desde
  antes desta sessão) — já é Postgres.

Trouxe pro branch de trabalho via `git merge origin/main` (mesmo padrão
das rodadas anteriores) e atualizei o mapa:

- Fluxo 03 (CRM/Escalonamento/Qualidade): o subgrafo de avaliação agora
  mostra o botão → rota → execução em background → progresso → polling,
  em vez do CLI isolado; nó deixou de ser estilizado como pendência.
- Tabela "Estado real por área": linha da avaliação automática passou de
  "parcial" pra "ativo"; nova linha "Áudio recebido do cliente" cobrindo o
  TASK-0209.
- Glossário de arquivos: `agentEvalRunStore.ts` adicionado, descrições de
  `qualityAudit.ts`, `agentEvalService.ts`, `scripts/eval-agent.ts` e
  `QualityAuditCenter.tsx` atualizadas.

### Validação (4ª rodada)

- `npm run lint` — limpo.
- `npx vitest run` — 240 arquivos / 1296 testes, todos passando.
- `npm run build` — limpo.

### Pendente/risco (4ª rodada)

- O próprio PR #587 registra que o botão não foi testado manualmente
  contra Supabase/Gemini reais (sessão sem acesso, egress restrito até
  03/09) — o mapa já sinaliza isso na legenda do fluxo 03.
- Mesmo risco de sempre: snapshot manual, desalinha com o tempo se
  ninguém regenerar.

## Atualização (mesma tarefa, 5ª rodada)

Usuário perguntou como o Artifact funciona, como compartilhar, e depois
pediu pra deixar o link fixo no `CLAUDE.md` e/ou na issue de status
(#504), atualizado conforme o projeto evolui.

Expliquei antes de agir: o Artifact é privado por padrão — só o dono da
conta consegue abrir; compartilhar exige uma ação manual dele no menu de
compartilhamento da própria página (eu não tenho como habilitar isso via
API). Deixei isso registrado no próprio `GUIA-DO-PROJETO.md` pra não virar
um link morto pra quem abrir sem saber disso.

### Mudança

- `docs/GUIA-DO-PROJETO.md`: seção "Mapa visual (onboarding)" agora
  também linka a URL pública do Artifact
  (`https://claude.ai/code/artifact/b7a564a3-024a-4b15-848a-c1323fbc1f4a`),
  com a ressalva de compartilhamento acima, e a instrução de que qualquer
  sessão que mudar `docs/mapa-do-projeto/index.html` deve republicar no
  mesmo link (mesma URL, sem criar um artefato novo).
- `CLAUDE.md`: um parágrafo curto logo abaixo da linha que já manda ler o
  `GUIA-DO-PROJETO.md` primeiro, apontando pra essa mesma seção — cobre o
  pedido do usuário de "deixar no CLAUDE.md", sem duplicar o texto
  inteiro (ponteiro, não cópia).
- Postado na issue #504 (protocolo de sincronização) um comentário
  consolidando as 5 rodadas desta tarefa (TASK-0207) com o link do mapa e
  do artefato, já que nenhuma rodada anterior tinha reportado lá ainda.

### Validação (5ª rodada)

- `npm run lint`, `npx vitest run` (240/1296) e `npm run build` — limpos
  (mudança é só documentação, sem tocar código de aplicação).

### Pendente/risco (5ª rodada)

- O link do Artifact só funciona pra terceiros depois que o dono da conta
  compartilhar manualmente — até lá, é utilizável só por quem já tem
  acesso à conta que publicou.
- Continua sendo atualização manual: uma sessão futura precisa lembrar de
  republicar o Artifact (mesma URL) toda vez que editar o HTML local,
  senão os dois ficam dessincronizados.

## Atualização (mesma tarefa, 6ª rodada — encerramento)

Usuário esclareceu o modelo de edição: hoje todo editor deste repositório
é uma sessão de Claude, então basta a regra ficar documentada — não é
preciso um mecanismo técnico separado de sincronização. Confirmou também
que o link do Artifact já está com compartilhamento habilitado.

### Mudança

`docs/GUIA-DO-PROJETO.md`: a seção "Mapa visual (onboarding)" ganhou uma
regra explícita — editar `docs/mapa-do-projeto/index.html` sem republicar
o Artifact na mesma URL conta como tarefa incompleta, não como passo
opcional à parte. Removida a ressalva de "se o link não abrir" (já
confirmado que o compartilhamento está ativo).

### Validação

`npm run lint`, `npx vitest run` (240/1296) e `npm run build` limpos.

### Encerramento desta tarefa

TASK-0207 concluída nesta rodada. Resumo completo já reportado na issue
#504 (comentário https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/504#issuecomment-5503099057) —
esta 6ª rodada só reforça a regra de sincronização, sem mudança de escopo.
