# TASK-0060 — Corrige atalho de tema no header que nunca incluia o modo Limpo no ciclo

- **Iniciado em:** 2026-08-25T04:30:18.771Z
- **Agente/plataforma:** Claude
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** concluído

## Objetivo

Corrige atalho de tema no header que nunca incluia o modo Limpo no ciclo

## Atualizações

Achado real de UI, reportado pelo dono do produto com print (seta apontando pro ícone de lua/sol no
canto superior direito do `Header.tsx`): esse ícone é um atalho de alternância RÁPIDA de tema,
distinto do seletor completo de 4 opções (Escuro/Claro/Limpo/Azul) que já existe no menu. O atalho
rápido (`toggleTheme`, `Header.tsx:72`) ciclava só `dark -> light -> blue -> dark` — o tema "Limpo"
(`clean`, adicionado numa sessão anterior, PR #405) nunca tinha sido inserido nesse ciclo. Resultado:
clicando repetidamente nesse ícone específico nunca era possível chegar no tema Limpo (só selecionando
manualmente no seletor completo de 4 botões, que já incluía `clean` corretamente).

- `src/components/Header.tsx` — `toggleTheme` agora cicla `dark -> light -> blue -> clean -> dark`.
- Sem teste automatizado cobrindo esse atalho especificamente (nenhum já existia); validado por
  leitura de código e pela suíte completa não ter regredido.
- Validado com `npm run lint` (limpo), `npx vitest run` (894 testes, 173 arquivos, todos verdes),
  `npm run build` (limpo).

## Encerramento

Ao concluir, atualize o status, informe os commits e as validações na issue #290 usando o identificador **TASK-0060**.

### Modelo de atualização na #290

```markdown
### TASK-0060 — Claude — 2026-08-25T04:30:18.771Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
