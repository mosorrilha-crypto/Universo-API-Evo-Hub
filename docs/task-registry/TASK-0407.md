# TASK-0407 — Corrige achado 3 do checklist de viabilidade comercial sobre human_handoff_rules

- **Iniciado em:** 2026-09-12T18:35:47.262Z
- **Agente/plataforma:** Claude
- **Issue central:** [#758](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/758)
- **Status:** concluído

## Objetivo

Corrigir `docs/CHECKLIST-VIABILIDADE-COMERCIAL.md` (criado em TASK-0405, mesclado em PR #775), que caracterizava incorretamente `human_handoff_rules` vazio como um "gap pequeno de auditoria" (~3 linhas de fix). A investigação feita em TASK-0406, antes de tentar implementar esse fix, mostrou que a premissa estava errada — ver `docs/task-registry/TASK-0406.md` para os detalhes técnicos completos.

## Atualizações

Editado `docs/CHECKLIST-VIABILIDADE-COMERCIAL.md`:

1. **Seção 1 (Generalização multi-vertical):** os 2 prompts hardcoded de estética em `autoReply.ts` (achado que estava `[ ]`) marcados como `[x]`, referenciando TASK-0406/PR #776 (mesclada). O critério de aceite de `docs/AGENTE-VERTICAL-ARQUITETURA.md` seção 8 também marcado `[x]` no checklist (a exceção concreta que faltava foi corrigida); nota explícita de que o checkbox formal naquele outro documento não foi mexido aqui, por ter processo próprio.
2. **Seção 2 (Onboarding self-service):** o item sobre `human_handoff_rules` vazio foi reescrito do zero. Trocado de "fix pequeno e independente, adicionar 1 finding" para "decisão de produto em aberto" — com os fatos que sustentam isso: o documento não tem campo editável no frontend (`VISUAL_KNOWLEDGE_BASE_DOCUMENT_TYPES` não o inclui), o backend trata seu tipo com um `case` vazio (`knowledgeBaseStore.ts:572-574`), é sempre `data: {}` para todos os tenants (não uma lacuna específica do Dr. Daniel), e a própria documentação do produto (`KnowledgeBaseDocumentation.tsx:108-111`) já o descreve como reservado para versão futura. Também ajustada a frase do item vizinho (gate de publicação da KB) que insinuava que o Dr. Daniel "não tinha configurado" regras de handoff — corrigido para deixar claro que isso não é hoje configurável por ninguém.

Nenhum código alterado — só o arquivo de documentação.

## Validação

- `npm run lint` — limpo.
- `npx vitest run` — 304 arquivos / 1742 testes, todos passando (sem alteração de código, suíte inalterada).
- `npm run build` — limpo (sem alteração de código).

### TASK-0407 — Claude — 2026-09-12

**Tarefa concluída:** corrigido o achado 3 (e atualizado o achado 2) de `docs/CHECKLIST-VIABILIDADE-COMERCIAL.md` para refletir a investigação real feita em TASK-0406 — `human_handoff_rules` vazio é uma decisão de produto em aberto (documento placeholder sem modelo de conteúdo, universal a todos os tenants), não um gap de auditoria corrigível com poucas linhas. Também marcados como concluídos os 2 itens da seção 1 que dependiam da correção já mesclada em TASK-0406/PR #776.

**Arquivos/commits:** `docs/CHECKLIST-VIABILIDADE-COMERCIAL.md`, `docs/task-registry/TASK-0407.md`.

**Validação:** `npm run lint` / `npx vitest run` / `npm run build`, todos limpos.

**Pendente ou risco:** nenhum risco técnico (só documentação). Fica em aberto, como já registrado no próprio checklist, a decisão de produto sobre desenhar campos estruturados de encaminhamento humano — não é uma correção de bug, é uma feature nova a avaliar quando fizer sentido.
