# TASK-0180 — Operador consegue liberar a IA na hora (sem esperar a janela de 5min) quando quer que ela retome uma conversa após responder manualmente

- **Iniciado em:** 2026-08-31T11:06:27.816Z
- **Agente/plataforma:** Claude
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** cancelada — duplicada pela TASK-0181 (parte 2), ver abaixo

## Objetivo

Operador consegue liberar a IA na hora (sem esperar a janela de 5min) quando quer que ela retome uma conversa após responder manualmente

## Atualizações

Achado real reportado pelo dono do produto em 31/08/2026, via conversa real com a lead "A.M&L STORE TIENDA ONL" (tenant Monique, phone `595981620462`, conversation_id `136bd89d-182f-45c2-8a05-51eeaef5a80c`): o gate do TASK-0177 (que pausa a IA por 5min após uma resposta manual do operador, pra evitar cruzamento IA/operador) não tinha nenhuma saída manual. Se a lead continuava respondendo dentro da janela de 5min — bem plausível numa conversa real ao vivo — o operador era forçado a responder manualmente de novo, o que RENOVAVA a janela, travando a conversa em modo manual permanente sem jeito de devolver o controle pra IA. Confirmado na timeline real: lead pergunta preço 10:41:40 → operador responde manualmente 10:46:43 → lead pergunta de novo 10:47:19 (36s depois, dentro da janela) → IA pausada por design → operador forçado a responder manualmente de novo 10:51:07, renovando a trava.

Implementei um fix completo (campo `operatorReleasedAiAt`, ação "Liberar IA agora" no menu ⋮, gate ajustado em `triggerAutoReply`, 2 testes novos, 5/5 passando, lint/suíte completa/build limpos) e cheguei a aplicar a migration em produção via `apply_migration` — mas fui interrompido antes de commitar/dar push. Ao retomar e sincronizar com `main`, descobri que a **TASK-0181 (parte 2)** já havia resolvido exatamente o mesmo achado (mesmo relato do dono do produto, mesmo gate, mesma lógica — coluna `operator_ai_release_at`, `ConversationStatePatch.releaseAiNow`, botão "Devolver a IA agora"), já mesclada em `main` antes do meu push. Auditei a implementação deles (não só confiei no "concluído"): reli o gate em `webhooks.ts`, o mapeamento em `conversationStore.ts`, o botão em `WhatsAppLeadsSim.tsx`, e rodei `server/routes/__tests__/webhooksOperatorActivePause.test.ts` (5/5 passando, incluindo os 2 casos que cobrem exatamente meu cenário) — confirmado que resolve o problema de verdade, não é um "concluído" só de nome.

Cancelando esta task como duplicada, sem commitar a implementação redundante. Único resíduo real: a migration que eu já tinha aplicado em produção (`conversations_operator_released_ai_pause`, coluna `operator_released_ai_at`) nunca teve o arquivo `.sql` commitado no repo — a própria TASK-0181 já tinha sinalizado essa coluna como "drift não explicado" no registro deles, sem saber que era minha. Resolvido nesta mesma task: `supabase/migrations/0074_drop_orphan_operator_released_ai_at.sql` remove a coluna órfã.

## Validação

- Implementação original (antes de descobrir o duplicado): `npx vitest run` no arquivo de teste — 5/5 passando; regressão via stash confirmada; `npm run lint`, `npx vitest run` completo (235 arquivos/1262 testes) e `npm run build` limpos.
- Auditoria da TASK-0181 (o que efetivamente ficou em produção): reli `webhooks.ts`/`conversationStore.ts`/`WhatsAppLeadsSim.tsx`, rodei os mesmos testes contra o código mesclado — 5/5 passando.
- Migration de limpeza `0074_drop_orphan_operator_released_ai_at.sql` aplicada via `apply_migration`.

## Encerramento

Ao concluir, atualize o status, informe os commits e as validações na issue #290 usando o identificador **TASK-0180**.

### Modelo de atualização na #290

```markdown
### TASK-0180 — Claude — 2026-08-31T11:06:27.816Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
