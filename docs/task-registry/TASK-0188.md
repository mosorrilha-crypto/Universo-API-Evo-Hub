# TASK-0188 — remove secao 3 caminhos elegi con confianza (atrito) e troca foto do hero por retrato real do usuario

- **Iniciado em:** 2026-09-01T13:36:52.299Z
- **Agente/plataforma:** Claude
- **Issue central:** [#504](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/504)
- **Status:** concluída

## Objetivo

remove secao 3 caminhos elegi con confianza (atrito) e troca foto do hero por retrato real do usuario

## Atualizações

Pedido direto do usuário, com prints reais da página publicada
(`/catalogo/monique-teste/novo`): a seção "Catálogo claro. Elegí con confianza."
mostrava 3 cards de "caminhos" (Resultado natural / Rutina más práctica /
Transformación completa) antes do catálogo real de serviços — o usuário
identificou isso como ponto de atrito (repete a mesma pergunta da Etapa 1 da
evaluación, sem nenhuma informação nova) e pediu a remoção. Pediu também para
trocar a foto do hero pelo retrato real que ele mesmo forneceu.

Correções em `src/components/PublicCatalogMoniqueConcierge.tsx`:
- Removida a constante `startingPoints`, o bloco JSX `.starting-points` (a
  seção dos 3 cards) e todo o CSS associado (`.starting-point*` no tema claro,
  no tema escuro "Black & Gold" e na media query mobile). A seção "Catálogo
  claro. Elegí con confianza." agora vai direto para o catálogo real
  (accordion por categoria, feature já existente na `main` de uma tarefa
  posterior à TASK-0131).
- Trocada a imagem do hero: em vez de sobrescrever `full-face.jpg` (que
  também é usado, com outro propósito, na galeria "Resultados reales" logo
  abaixo — sobrescrever teria afetado os dois lugares sem querer), foi criado
  um arquivo próprio `public/monique-novo/hero-retrato.jpg` só para o hero,
  com o retrato enviado pelo usuário.

Nota de processo: entre a TASK-0131 (última mudança nesta área) e agora,
outra sessão evoluiu bastante essa mesma seção na `main` (categorias em
accordion, `isMicropigmentacion`, `groupProductsByCategory` — TASK mais
recente que a #131). A branch local precisou ser reconstruída a partir da
`main` atual (`git checkout -B ... origin/main`) e as mudanças anteriores
(stash) reaplicadas com um conflito real de merge no próprio arquivo,
resolvido preservando a nova estrutura de accordion e removendo só o bloco
dos 3 caminhos de dentro dela.

## Encerramento

**Validação:** `npm run lint`, `npx vitest run` (237 arquivos, 1273 testes) e
`npm run build`, todos limpos. Validação visual real via Playwright/dev local
(desktop, full-page): confirmado hero com a nova foto e o selo "01" mantido,
seção "Elegí con confianza" indo direto pro accordion de categorias sem os 3
cards antigos, e a galeria "Resultados reales" com a foto original intacta
(não afetada pela troca).

**Pendente ou risco:** nenhum no código. Validação foi feita com um mock do
endpoint público (Supabase não configurado localmente); vale conferir a
página real em produção após o deploy.

### Modelo de atualização na #504

```markdown
### TASK-0188 — Claude — 2026-09-01T13:36:52.299Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
