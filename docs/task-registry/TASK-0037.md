# TASK-0037 — Tornar visível o link de geração do token de gerenciamento Meta e orientar os campos obrigatórios

- **Iniciado em:** 2026-08-24T03:02:24.470Z
- **Agente/plataforma:** Manus AI
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** em revisão

## Objetivo

Tornar visível o link de geração do token de gerenciamento Meta e orientar os campos obrigatórios

## Atualizações

A captura mostrou que o link do token de gerenciamento não estava no código da `main` atualmente servido, apesar de ter existido na branch da implementação anterior. A correção adiciona um link visível no mesmo padrão do link de leitura, além de links para o Graph API Explorer, o token de usuário do sistema para produção e o Access Token Debugger.

Os seis campos exibidos nas capturas pertencem a dois recursos diferentes. Para a Central de Anúncios, são necessários a conta de anúncios, o token de leitura e o token de gerenciamento; o token de leitura pode ser mantido se já estiver configurado. Para a CAPI, o Meta Pixel ID e o Access Token Graph API são necessários para enviar eventos; o Test Event Code é opcional e serve apenas para testes no Meta Test Events. Não é necessário preencher os seis campos para ativar o gerenciamento de campanhas.

Arquivos alterados: `src/components/TrafficCenter.tsx`. A documentação da Central já contém os links oficiais. A correção foi validada com `npm run lint`, `npm run build` e `git diff --check`.

Registre aqui decisões relevantes, arquivos alterados e bloqueios durante a execução.

## Encerramento

Ao concluir, atualize o status, informe os commits e as validações na issue #290 usando o identificador **TASK-0037**.

### Encerramento da implementação

**Tarefa concluída:** o link de geração do token de gerenciamento agora aparece diretamente abaixo do campo, no mesmo padrão do link de leitura. Também foram adicionados links de produção para System Users e validação pelo Access Token Debugger.

**Arquivos/commits:** `src/components/TrafficCenter.tsx` — commit [85da895](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/commit/85da895). A branch da correção será publicada em PR separada.

**Validação:** `npm run lint`, `npm run build` e `git diff --check` aprovados. O build mantém somente o aviso não bloqueante de chunk JavaScript acima de 500 kB.

**Pendente ou risco:** publicar a branch e abrir PR para revisão humana. Depois do merge, o Render precisará concluir o deploy para o link aparecer no navegador. Nenhum token foi coletado ou armazenado durante esta correção.

### Modelo de atualização na #290

```markdown
### TASK-0037 — não informado — 2026-08-24T03:02:24.470Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
