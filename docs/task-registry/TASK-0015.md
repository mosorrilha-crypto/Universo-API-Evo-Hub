# TASK-0015 — corrigir corte móvel do menu de respostas rápidas

- **Iniciado em:** 2026-08-23T13:23:00.375Z
- **Agente/plataforma:** Manus.Ai
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** concluído; correção responsiva implementada

## Objetivo

corrigir corte móvel do menu de respostas rápidas

## Atualizações

A captura móvel confirmou que o menu ancorado com `right-0` ultrapassava a borda esquerda da viewport, cortando o início dos textos. O menu agora usa posicionamento fixo e centralizado no celular, com margens laterais, largura máxima responsiva e limite de altura baseado em `dvh`; no desktop mantém a abertura acima do botão. A área rolável também usa `overscroll-contain` para não deslocar a página ao navegar pelas respostas.

## Encerramento

Ao concluir, atualize o status, informe os commits e as validações na issue #290 usando o identificador **TASK-0015**. Arquivos: `src/components/chat/QuickRepliesMenu.tsx` e este registro.

## Validação

`git diff --check`, `npm run lint` e `npm run build` passaram. O build de produção de frontend e backend foi concluído; permanece apenas o aviso não bloqueante de chunk JavaScript acima de 500 kB. A captura fornecida pelo usuário foi usada como evidência do problema móvel e a correção foi aplicada sem alterar respostas ou dados persistidos.

### Modelo de atualização na #290

```markdown
### TASK-0015 — Manus.Ai — 2026-08-23T13:23:00.375Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
