# TASK-0301 — Reformula navegação: Atendimento vira home, remove Conversas/CRM/Agenda/Financeiro do menu superior, move CRM/Agenda/Financeiro pra caixa de ferramentas do Atendimento (mobile), audita botões do menu ⋮ da conversa

- **Iniciado em:** 2026-09-05T11:42:30.116Z
- **Agente/plataforma:** Claude
- **Issue central:** [#504](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/504)
- **Status:** concluída

## Objetivo

Reformula navegação: Atendimento vira home, remove Conversas/CRM/Agenda/Financeiro do menu superior, move CRM/Agenda/Financeiro pra caixa de ferramentas do Atendimento (mobile), audita botões do menu ⋮ da conversa

## Atualizações

Pedido direto do dono do produto, em duas partes, com 3 prints reais (menu hamburger mobile marcado à mão riscando "Conversas", barra inferior do Atendimento circulada, menu ⋮ de uma conversa real).

**1. Auditoria do menu ⋮ da conversa (nenhuma mudança de código — só investigação).** Rastreei os 12 itens até o backend: todos funcionam, incluindo "Devolver a IA agora" (`PATCH /api/conversations/:phone/state {releaseAiNow:true}` → `operator_ai_release_at` → `webhooks.ts:156-157` pula a pausa de 5min pós-resposta manual). Único ponto de atenção (não é bug): "Sugerir melhoria"/"Reportar bug" dependem de `requireQualityModule()` — só funcionam se o módulo Qualidade estiver ligado pro tenant.

**2. Reformulação de navegação** — decisões confirmadas com o dono do produto via 3 perguntas diretas antes de codar:
   - Atendimento (`whatsapp`) vira a tela padrão do sistema — o painel "Hoje" (`OperationsHomeWorkspace`/`OperationsCenter`, seções Prioridades/Atalhos/Configurar) saiu do ar. Prioridades e Atalhos foram excluídos por pedido direto ("vamos refinar isso futuramente"); só a etapa "Configurar" (checklist de ativação do tenant — canal, contexto, agenda, serviços) sobrevive, agora dentro da aba Empresas (`TenantActivationChecklist`, antes só visível pra quem via a Home).
   - CRM (Vendas), Agenda e Financeiro saíram do menu superior por completo — só ficam acessíveis pela caixa de ferramentas ("Ferramentas", gaveta mobile) dentro do Atendimento, atrás dos novos `onGoToCrm`/`onGoToFinancial` (mesmo padrão já existente de `onGoToAgenda`/`onGoToEscalations`).
   - Escopo confirmado como só mobile por enquanto — desktop não tem gaveta de Ferramentas (removida na TASK-0225); CRM/Agenda/Financeiro continuam alcançáveis lá só pelos fluxos que já existiam (URL direta / links internos), sem um novo ponto de entrada no cabeçalho desktop.
   - "Home" foi removido do tipo `ActiveTab` (não só desativado) — `parseStoredActiveTab` migra qualquer preferência antiga salva (`home`) pra `whatsapp`, mesmo padrão já usado pra `integration`/`agenda_financeiro`.
   - Logo "Universo" do cabeçalho (mobile e desktop) virou um botão que leva de volta ao Atendimento — reforça a saída de "Conversas" do menu, complementando o `FloatingAttendanceButton` (atalho flutuante já existente, some só na própria aba Atendimento).

**Arquivos:** `src/types.ts`, `src/lib/activeTab.ts` (+ teste), `src/App.tsx`, `src/components/Header.tsx` (+ teste `Header.empresasMenu.test.tsx` atualizado — o teste antigo de "esconde Financeiro sem esconder Agenda no topo" perdeu o objeto, nenhum dos dois existe mais lá), `src/components/WhatsAppLeadsSim.tsx` (novas props `onGoToCrm`/`onGoToFinancial`, tiles na gaveta Ferramentas). Excluídos por completo (ficaram sem nenhuma referência): `src/components/OperationsHomeWorkspace.tsx`, `src/components/OperationsCenter.tsx`, `src/components/__tests__/OperationsHomeWorkspace.test.tsx`.

**Validação:**
- `npm run lint` limpo; `npx vitest run` — 270 arquivos / 1486 testes (só o teste do menu superior precisou de ajuste pro novo layout, coberto acima); `npm run build` limpo.
- Validação visual real não foi possível nesta sessão — rede externa deste ambiente segue bloqueada (achado de sessões anteriores), sem bypass de login em modo demo. Validação por leitura de código ponta a ponta (rotas, stores, testes de render) em vez de clique real no navegador.

**Pendente ou risco:** nenhum de código. Recomendo confirmação visual do dono do produto em produção (celular) assim que possível — logo clicável, sumiço de Conversas/CRM/Agenda/Financeiro do menu superior, novos atalhos "Vendas"/"Agenda completa"/"Financeiro" na gaveta Ferramentas, e o checklist de ativação agora dentro de Empresas.

## Encerramento

Ao concluir, atualize o status, informe os commits e as validações na issue #504 usando o identificador **TASK-0301**.

### Modelo de atualização na #504

```markdown
### TASK-0301 — Claude — 2026-09-05T11:42:30.116Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
