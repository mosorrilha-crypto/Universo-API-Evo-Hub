# TASK-0394 — Otimiza custo de contexto entre sessões: rotaciona issue #504, compacta GUIA-DO-PROJETO.md

- **Iniciado em:** 2026-09-11T23:17:20.706Z
- **Agente/plataforma:** Claude
- **Issue central:** [#758](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/758) (rotada nesta mesma tarefa — ver abaixo)
- **Status:** concluído

## Objetivo

Otimiza custo de contexto entre sessões: rotaciona issue #504, compacta GUIA-DO-PROJETO.md

## Atualizações

Pedido direto do dono do produto, depois de uma pergunta sobre quanto contexto uma sessão do Claude Code precisa reler a cada resposta: "otimiza e compila as fontes de informação de cada nova conversa, veja o que já está obsoleto e resume o que está muito grande, e apaga registros antigos e redundantes".

Levantamento de tamanho real das fontes lidas por toda sessão nova (`docs/*.md`, `docs/task-registry/`): `docs/GUIA-DO-PROJETO.md` (leitura mandatória, "leia sempre primeiro") estava com **90KB/306 linhas** — quase 60KB só na seção "Mudanças recentes relevantes" (changelog narrativo, redundante com a tabela de status acima dela e com `docs/task-registry/TASK-XXXX.md` de cada item). A issue #504 (painel de handoff, também leitura mandatória) já estava com **244 comentários em 14 dias** (~17/dia) — pior ritmo que o gatilho que causou a própria rotação #290→#504 (149 em ~10 dias).

**3 ações:**
1. **Rotação #504 → #758** (mesmo padrão já usado #290→#504, já documentado como política esperada no próprio CLAUDE.md): issue nova criada, comentário-ponteiro postado na #504, #504 fechada como `completed` (nunca reaberta pra novo registro, preservada como histórico read-only — mesmo tratamento da #290). `CLAUDE.md`, `docs/GUIA-DO-PROJETO.md` (seção "Protocolo de sincronização") e `.github/pull_request_template.md` atualizados pra apontar pra #758.
2. **Compactação da seção "Mudanças recentes relevantes" de `GUIA-DO-PROJETO.md`**: ~140 linhas de prosa viraram ~16 linhas (uma por tarefa/grupo de tarefas relacionadas), cada uma com data + TASK-ID + resultado em 1-2 frases + pointer pro `docs/task-registry/TASK-XXXX.md` correspondente pra detalhe completo. Itens totalmente superados por uma tarefa posterior (ex.: TASK-0218/TASK-0307, ambos fechados pela TASK-0327) foram fundidos numa única linha "arco fechado". Itens com risco/pendência ainda real (TASK-0249: 4 dos 5 pontos de criptografia em repouso ainda faltam) preservam essa informação explicitamente — não é só corte, é curadoria. A tabela "Estado real por área" (a parte que mais importa) e as demais seções não foram tocadas.
3. **`docs/task-registry/` (389 arquivos, 2.3MB) — decisão explícita de NÃO apagar.** É a mesma classe de trilha de auditoria que o projeto já trata como permanente (igual à #290/#504 fechadas, nunca apagadas) — apagar quebraria o protocolo de auditoria já documentado no CLAUDE.md ("reconstruct what actually happened from... the matching docs/task-registry/TASK-XXXX.md"). Cada sessão só lê o arquivo específico da tarefa que está tocando, não a pasta inteira — o custo por conversa já é próximo de zero; o volume total é custo de armazenamento no repositório, não de contexto por sessão.

Os ~25 docs avulsos e datados em `docs/` (auditorias de Meta Ads, notas de CRM/agenda, etc.) não foram tocados: nenhum deles é leitura mandatória pra uma sessão nova (só `GUIA-DO-PROJETO.md`, `CLAUDE.md` e a issue-painel são), então não pesam no custo por conversa — ficam como candidatos a uma limpeza futura separada, se o dono do produto quiser.

**Arquivos:** `CLAUDE.md`, `docs/GUIA-DO-PROJETO.md`, `.github/pull_request_template.md`. Issue [#758](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/758) criada; [#504](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/504) fechada.

**Validação:** mudança é só documentação (nenhum arquivo de código tocado) — `npm run lint`/`npx vitest run`/`npm run build` continuam passando (nada no código foi alterado). Revisão manual: `git diff --stat` confirma que só a seção-alvo de `GUIA-DO-PROJETO.md` foi tocada (a tabela "Estado real por área" e as demais seções ficaram intactas).

**Pendente ou risco:** nenhum risco técnico (não é área crítica — não toca `tenant_id`/auth/pagamento/mensageria). Efeito colateral esperado e aceito: qualquer link/bookmark apontando pra #504 como "painel atual" precisa ser atualizado manualmente pra #758 por quem o guarda (o comentário de rotação na própria #504 cobre isso pra quem chegar lá primeiro).

## Encerramento

Ao concluir, atualize o status, informe os commits e as validações na issue #758 usando o identificador **TASK-0394**.

### Modelo de atualização na #758

```markdown
### TASK-0394 — Claude — 2026-09-11T23:17:20.706Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
