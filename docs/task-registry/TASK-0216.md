# TASK-0216 — Aumenta escala da barra inferior mobile do Atendimento (ícones e rótulos ainda pequenos/desproporcionais)

- **Iniciado em:** 2026-09-02T17:42:35.081Z
- **Agente/plataforma:** Claude
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** concluído

## Objetivo

Aumenta escala da barra inferior mobile do Atendimento (ícones e rótulos ainda pequenos/desproporcionais)

## Atualizações

Follow-up direto da TASK-0213 (PR #592): depois de confirmar que a
duplicação da barra inferior estava corrigida, o dono do produto mandou
outro print (celular real, `.atendimento-bottom-nav`) apontando que a
escala dos ícones/rótulos ainda estava pequena/desproporcional perto do
resto da UI (ex: texto da lista de conversas ao lado).

Mesmo espírito do ajuste já feito no menu hambúrguer mobile (TASK-0195):
- `.atendimento-bottom-nav__item` (`index.css`): rótulo `.68rem` -> `.75rem`,
  padding vertical `.5rem`/`.55rem` -> `.6rem`/`.65rem` (toque mais
  generoso), gap `.2rem` -> `.25rem`.
- Ícones no JSX (`WhatsAppLeadsSim.tsx`, os 4 botões da nav): `w-[22px]
  h-[22px]` -> `w-6 h-6` (24px).
- Badge de contagem (`.atendimento-bottom-nav__badge`) escalado
  proporcionalmente junto.

Arquivos alterados: `src/index.css`, `src/components/WhatsAppLeadsSim.tsx`.

## Encerramento

**Status: concluído.** Ajuste pequeno e isolado (só valores de escala,
mesmo padrão já usado antes nesta mesma barra). Sem self-merge, mesma
cautela das últimas rodadas nesta tela.

**Validação:** `npm run lint`, `npx vitest run` (243/243 arquivos,
1311/1311 testes) e `npm run build`, todos limpos.

**Pendente ou risco:** confirmar em dispositivo real que a nova escala
resolve a queixa de "desproporcional" — não há como medir "proporção
percebida" sem ver renderizado; se ainda não bater, é rápido de ajustar de
novo (mesmos dois arquivos).

### Modelo de atualização na #290

```markdown
### TASK-0216 — Claude — 2026-09-02T17:42:35.081Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
