# TASK-0072 — Tela de gestão de tenants: bloqueio de acesso, troca de login/senha, histórico de pagamento mensal

- **Iniciado em:** 2026-08-25T17:50:22.686Z
- **Agente/plataforma:** Claude
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** em andamento

## Objetivo

Tela de gestão de tenants: bloqueio de acesso, troca de login/senha, histórico de pagamento mensal

## Atualizações

Pedido direto de chat, sem issue própria. Escopo decidido em conversa antes de codar
(via pergunta explícita ao usuário): isolamento reforçado no MESMO banco (RLS), não
banco Supabase separado por tenant — mais barato e sem custo por cliente novo; tela
restrita a saas_admin (uso interno, não self-service pro tenant).

Entregue:
- `supabase/migrations/0048_tenant_access_block_and_billing.sql` — `tenants.is_active`
  (bloqueio de acesso reversível) + tabela `tenant_billing_records` (histórico de
  cobrança mensal do Universo ao tenant, registro manual sem gateway, RLS por
  tenant_id no mesmo padrão de `financial_transactions`). **Aplicada em produção**
  via Supabase MCP `apply_migration` (projeto `pkocepjfedtsxmufymvd`), confirmada em
  `list_migrations` (nome `tenant_access_block_and_billing`).
- `server/routes/auth.ts` — login recusado quando `tenants.is_active = false`,
  checado SÓ depois da senha validar (nunca antes, pra não virar oráculo de "esse
  tenant existe/está bloqueado" pra quem não tem senha).
- `server/routes/admin.ts` — `PATCH /api/admin/tenants/:id` aceita `isActive`;
  `PATCH /api/admin/operators/:id` estendido pra aceitar `email`/`name`/`password`
  (senha vira hash bcrypt), além da `role` que já suportava; rotas novas
  `GET/POST /api/admin/tenants/:id/billing` e `PATCH/DELETE .../billing/:recordId`.
- `src/components/SaaSAdminDashboard.tsx` + `src/hooks/useRealTenants.ts` — botões
  Bloquear/Reativar e Pagamentos na lista de tenants (mobile+desktop), modal de
  histórico de pagamento (registrar/marcar pago-pendente-atrasado/apagar), botão
  de trocar login/senha na lista de usuários.

**Achado separado, fora do escopo desta tarefa (registrado aqui pra rastreabilidade):**
`list_migrations` no Supabase de produção mostra uma migration `public_catalog_template_by_tenant`
aplicada em 25/08/2026 (mesmo dia) sem NENHUM arquivo correspondente em `supabase/migrations/`
em todo o histórico do Git (`git log --all` não encontra o nome em lugar nenhum). Alguém
aplicou uma migration direto via MCP/SQL Editor sem nunca commitar o arquivo — exatamente o
tipo de gap que o incidente da issue #93 already documentou. Não investiguei o conteúdo dela
(fora do escopo desta tarefa) — só sinalizando pra quem for investigar depois.

## Encerramento

**Status: concluído.** Commits/validação: ver comentário na issue #290.

### Modelo de atualização na #290

```markdown
### TASK-0072 — Claude — 2026-08-25T17:50:22.686Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
