# TASK-0254 — Audita lote de 19 achados pendentes da avaliação automática, fecha os legítimos e as reprovações inválidas do avaliador

- **Iniciado em:** 2026-09-03T20:55:41.015Z
- **Agente/plataforma:** Claude
- **Issue central:** [#504](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/504)
- **Status:** concluída

## Objetivo

Pedido direto do dono do produto: em vez de só corrigir os padrões mais
óbvios do lote de 19 achados pendentes (feito na TASK-0253), auditar cada
um individualmente confrontando a RESPOSTA REAL do atendente (não só o
resumo do achado) contra o motivo de reprovação do revisor/julgador, pra
confirmar se cada reprovação foi legítima antes de corrigir e fechar.

## Atualizações

Usei `quality_reviews.original_value`/`context` (guarda a resposta real e
a pergunta sintética de cada achado, mesmo pros criados antes da
`agent_eval_run_cases` existir) pra ler o texto exato de cada uma das 19
respostas reprovadas. Resultado da auditoria:

### Reprovações LEGÍTIMAS, corrigidas nesta task ou na TASK-0253 (fechadas como `resolved`)
- `d9cf189a`, `793a2b8d`, `dd06bc38` — agente avançou pra agenda sem pedir
  o nome. Corrigido na TASK-0253 (regra 24).
- `261fba02`, `6b3f2493` — agente pediu o nome numa pergunta puramente
  informativa (localização, preço) onde isso NÃO é exigido — o espelho
  exato do bug da TASK-0253, também coberto pela cláusula "fora desse
  momento... não pergunte à toa" da mesma regra 24.
- `06ebd668`, `e3e25758` — julgador reprovou como "robótico"/"formal
  demais" o próprio comportamento correto de pedir o nome no primeiro
  contato. Corrigido na TASK-0253 (exceção na regra 5 do julgador).
- `435369d6` — revisor de segurança exigiu espanhol quando a mensagem da
  cliente ("Quero marcar o combo de cejas y pestañas...") tem português
  predominante — decisão errada pela regra já corrigida na TASK-0248
  (idioma predominante). Achado anterior à correção (14:04 vs TASK-0248
  concluída ~18:10), já coberto.
- `1bbccd09`, `8a171fad` — abertura com afirmação de entusiasmo fora da
  lista de exemplos da regra 17 ("Com certeza!", "Sem problema!" antes de
  conteúdo transacional). Regra 17 ampliada nesta task pra cobrir
  qualquer afirmação/confirmação curta usada como abertura com exclamação,
  não só a lista fixa de palavras óbvias — `server/services/autoReply.ts`.

### Reprovações ILEGÍTIMAS — alucinação do revisor/julgador, resposta real já estava correta (fechadas como `rejected`, achado inválido, sem correção de código)
- `5c61acdb` — revisor alegou "serviço genérico sem técnica específica" e
  "sem pedir nome", mas a resposta real já cita "Diseño Tradicional con
  Hilo" (técnica específica) E já pergunta "¿Me decís tu nombre...?" na
  mesma frase. Contradiz o próprio texto avaliado.
- `b9cf3eed` — revisor alegou que o rascunho avançou "sem perguntar ou
  confirmar o nome", mas a resposta abre com "Oi, Camila!" — a cliente já
  tinha dito o nome dela na própria mensagem e o agente usou.
- `08ab618d` — revisor alegou que o agente "respondeu em espanhol" pra
  uma cliente que escreveu em português, mas a resposta real está 100%
  em português, do início ao fim — alegação diretamente oposta ao texto.
- `fc42b814` — julgador alegou "mistura de idioma entre português e
  espanhol na resposta", mas a resposta real está inteiramente em
  português, nenhuma palavra em espanhol.

Achados de tasks anteriores (TASK-0235 a 0248) já tinham documentado essa
classe de alucinação pontual do avaliador (ex: "con hilo" citado como
mistura de idioma, "Gs 550.000" citado como português) — esses 4 casos
são a mesma classe de erro, sem padrão novo que justifique mais uma
mudança de prompt agora; fechar como achado inválido é a ação certa (não
"corrigir" um bug que não existe na resposta avaliada).

### Continuam PENDENTES — reprovação legítima, sem lacuna de instrução nova identificada (mantidas)
- `11d348a2`, `e925ab43`, `6ef689d2` — regra 14 (nunca avançar pra agenda
  depois de pergunta puramente informativa) violada pelo próprio agente;
  já maximamente instruída, inconsistência probabilística conhecida desde
  TASK-0248.
- `8ed73fb7` — abertura promocional/opinativa longa antes de ir direto à
  disponibilidade; plausível mas sem evidência forte de bug de instrução
  específico, deixado pendente por falta de confiança pra generalizar.
- `f6d8b76b` — queixa de "repete saudação e introdução" pra uma resposta
  de primeiro contato que não parece repetir nada de fato (histórico
  vazio, uma saudação só); mantido pendente por incerteza, não fechado
  como inválido por falta de uma contradição tão clara quanto os 4 casos
  fechados acima.

Depois de mesclado o código, marquei os status reais em `quality_reviews`
(Supabase, tabela usada pela Central de Qualidade) via SQL direto,
mesmo padrão de campos que a rota `PATCH /api/quality-audit/reviews/:id`
usa (`status`, `review_note`, `updated_at`) — 10 pra `resolved` com nota
explicando a correção, 4 pra `rejected` com nota explicando a
contradição encontrada, 5 continuam `pending`.

## Encerramento

`npm run lint` limpo, `npx vitest run` — 258 arquivos / 1419 testes
passando (1 teste ajustado pra bater com o texto novo da regra 17), `npm
run build` limpo.

**Arquivos/commits:** branch `claude/gestor-refinamento-atendimento-mwu1a7`
— `server/services/autoReply.ts` (regra 17 ampliada),
`server/services/__tests__/autoReply.test.ts` (assert ajustado). Estados
de `quality_reviews` atualizados diretamente via Supabase MCP (dado, não
código — sem PR).

**Pendente ou risco:** 5 achados seguem pendentes de propósito (regra 14
já maximamente instruída, sem lacuna nova identificada; 2 casos de baixa
confiança pra generalizar). Recomendo mais uma rodada de avaliação
automática pra confirmar que os padrões corrigidos (nome antes de agenda,
nome desnecessário em pergunta informativa, "robótico" por perguntar
nome, "Com certeza!"/"Sem problema!") não reaparecem.

Relatado na issue #504 com o identificador **TASK-0254**.

### Modelo de atualização na #504

```markdown
### TASK-0254 — Claude — 2026-09-03T20:55:41.015Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
