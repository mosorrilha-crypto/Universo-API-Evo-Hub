# TASK-0343 — Ajustes finos na gaveta Ferramentas: Idioma/Tema recolhidos, Anúncios dentro do Status, CRM, Notificações em Configurações, Agenda vira página no mobile

- **Iniciado em:** 2026-09-08T12:58:05.710Z
- **Agente/plataforma:** Claude
- **Issue central:** [#504](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/504)
- **Status:** concluída — aguardando confirmação visual do dono do produto antes do merge

## Objetivo

Ajustes finos na gaveta Ferramentas: Idioma/Tema recolhidos, Anúncios dentro do Status, CRM, Notificações em Configurações, Agenda vira página no mobile

## Contexto

Sequência de pedidos diretos, com prints anotados, sobre a gaveta "Ferramentas" (Atendimento, mobile) logo depois da TASK-0342:

1. Idioma/Tema (TASK-0342 tinha deixado sempre visível, 6 pills numa fileira) — pedido pra "recolher os inativos, só aparecem quando solicitados", mas mantendo o visual de PILL com texto (não o ícone redondo isolado da TASK-0336).
2. "Somente anúncios" é conceitualmente parte do status de atendimento do agente — juntar na mesma seção.
3. "Vendas" (módulo) renomear pra "CRM".
4. "Notificações push" mudar da caixa "Empresa ativa"/"Sair" pra dentro de "Configurações".
5. Botão "Agenda" da barra inferior (mobile) abria um popup pequeno (`UpcomingEventsPanel`) em vez de navegar pra página completa de Agenda (que já existe, com a mesma barra inferior, igual Pendências).
6. Achado relacionado, já coberto: o popup de Ferramentas/Agenda "cortando os ícones" no mobile — causa raiz já corrigida na TASK-0338 (commit local `c34a4e5`, ainda não mergeado em `main` até este ponto), incluída no mesmo lote.

## Mudança

`src/components/WhatsAppLeadsSim.tsx`:
- **Status do agente**: ganhou uma 4ª pill ("Anúncios", toggle liga/desliga) na mesma fileira de Ativo/Restrito/Pausado — "Somente anúncios" deixou de ser um tile em círculo numa fileira própria.
- **Idioma e tema**: novo estado `expandedLangOrTheme` (`'language' | 'theme' | null`) — cada um vira uma pill mostrando só o valor ATUAL (ex: "PT", "Escuro") com uma setinha; tocar expande as opções logo abaixo como pills de texto (idioma) ou pills com ícone+rótulo (tema), escolher uma aplica e recolhe de volta.
- **Módulos**: label "Vendas" → "CRM".
- **Configurações**: "Notificações push" (toggle Ativar/Ativas) mudou da caixa "Empresa ativa"/"Sair" pra dentro da seção expansível "Configurações", como uma linha própria abaixo da grade de ícones.
- **Barra inferior, botão "Agenda"**: agora prioriza `onGoToAgenda()` (navega pra página completa `AgendaWorkspace`, `activeTab==='agenda'`) — só cai pro popup/fluxo de conectar Google Calendar quando essa prop não está disponível (usuário sem permissão pra aba Agenda).

`src/components/__tests__/WhatsAppLeadsSim.toolsLanguageTheme.test.tsx`: ajustado pro novo fluxo — precisa tocar na pill do valor atual ("PT"/"Escuro") antes de escolher outra opção.

## Encerramento

`npm run lint`, `npx vitest run` (276 arquivos / 1511 testes) e `npm run build` limpos.

**Arquivos:** `src/components/WhatsAppLeadsSim.tsx`, `src/components/__tests__/WhatsAppLeadsSim.toolsLanguageTheme.test.tsx`.

**Pendente ou risco:** mudança de UX vista pelo usuário final — enviei prévia visual estática pro dono do produto confirmar antes de abrir PR/mergear, mesmo padrão de cautela desta sessão. Vai junto com o commit pendente da TASK-0338 (correção real do bug de popup cortado), ainda não mergeado até este ponto.

Ao concluir, atualize o status, informe os commits e as validações na issue #504 usando o identificador **TASK-0343**.

### Modelo de atualização na #504

```markdown
### TASK-0343 — Claude — 2026-09-08T12:58:05.710Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
