# TASK-0032 — agrupar cópia da Base de Conhecimento em Modelos de negócio

- **Iniciado em:** 2026-08-24T02:03:36.234Z
- **Agente/plataforma:** Manus.Ai
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** concluída

## Objetivo

agrupar cópia da Base de Conhecimento em Modelos de negócio

## Atualizações

- A ação **Copiar Base de Conhecimento de outra empresa** foi retirada do fluxo principal da página e passou a ficar dentro do painel recolhível **Modelos de negócio e cópia de base para novo tenant**.
- O fluxo funcional foi preservado: seleção da empresa de origem, botão **Carregar nesta base**, indicador de carregamento e preenchimento do formulário com produtos, regras, FAQs e mensagem inicial.
- A cópia continua protegida pela permissão `canUseBusinessTemplates`, mantendo a restrição administrativa existente; não houve alteração de endpoint, dados reais ou persistência automática.
- A mudança reduz a altura visual da página e mantém as duas ações de configuração inicial no mesmo contexto operacional.

## Arquivos alterados

- `src/components/AgentKnowledgeBase.tsx`
- `docs/task-registry/TASK-0032.md`

## Validação

- `git diff --check` passou.
- `npm run lint` passou (`tsc --noEmit`).
- `npm test` passou: 165 arquivos e 858 testes.
- `npm run build` passou para frontend e backend.
- O build mantém apenas o aviso não bloqueante conhecido de chunk JavaScript acima de 500 kB.

## Encerramento

Ao concluir, informe o commit e as validações na issue #290 usando o identificador **TASK-0032**. Recomenda-se conferir visualmente o painel recolhível no celular com um usuário `saas_admin`.

### Modelo de atualização na #290

```markdown
### TASK-0032 — Manus.Ai — 2026-08-24T02:05:53Z

**Tarefa concluída:** a caixa de cópia da Base de Conhecimento foi agrupada dentro do painel recolhível de Modelos de negócio, junto aos presets para configuração de novos tenants. A ação continua disponível apenas no fluxo administrativo autorizado e mantém o seletor, carregamento, feedback e preenchimento do formulário.

**Arquivos/commits:** `src/components/AgentKnowledgeBase.tsx` e este registro.

**Validação:** `git diff --check`, `npm run lint`, `npm test` com 165 arquivos/858 testes e `npm run build` passaram. O build mantém apenas o aviso não bloqueante de chunk JavaScript acima de 500 kB.

**Pendente ou risco:** falta apenas conferência visual no celular com perfil `saas_admin`; nenhuma alteração financeira, de integração, endpoint ou persistência foi feita.
```
