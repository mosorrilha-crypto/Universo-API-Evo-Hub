# TASK-0035 — auditar seta publicada e remover Integrações da navegação

- **Iniciado em:** 2026-08-24T02:44:44.717Z
- **Agente/plataforma:** Manus.Ai
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** concluída

## Objetivo

Corrigir a versão efetivamente exibida no celular: tornar visível e funcional a seta de troca de empresa no cartão **Empresa ativa** do menu mobile e remover a página Integrações da navegação do produto.

## Atualizações

A captura confirmou que o controle ainda não aparecia no cartão Empresa ativa do Header mobile. A implementação anterior havia sido aplicada ao frame da Central, mas o usuário estava observando o cartão do menu móvel; por isso o Header agora também renderiza o seletor no ponto visual correspondente, usando a mesma lista real de tenants e o callback persistente existente. O menu permanece restrito a `saas_admin`, exige mais de um tenant, possui foco visível e fecha por clique externo, Escape ou seleção.

A página Integrações foi retirada da navegação administrativa e da renderização do App. O componente de guia permanece no código para não apagar implementação sem necessidade, mas deixa de ser uma tela acessível pelo produto. Preferências antigas salvas como `integration` são migradas para `whatsapp` no carregamento, evitando que um refresh reabra a página removida. O valor também foi retirado da união tipada `ActiveTab`.

## Encerramento

- **Arquivos:** `src/App.tsx`, `src/components/Header.tsx`, `src/lib/activeTab.ts`, `src/lib/activeTab.test.ts`, `src/types.ts` e este registro.
- **Commit:** será informado após a criação e publicação do commit.
- **Validação:** `git diff --check`, `npm run lint`, `npm test` — 166 arquivos e 860 testes aprovados — e `npm run build` concluídos. O build mantém apenas o alerta conhecido de chunk JavaScript acima de 500 kB.
- **Permissão e risco:** a troca continua limitada a `saas_admin`; nenhum endpoint, `tenant_id` ou regra de isolamento foi alterado. Falta confirmação visual do usuário no celular após o deploy público.

### Modelo de atualização na #290

```markdown
### TASK-0035 — Manus.Ai — 2026-08-24

**Tarefa concluída:** corrigido o ponto efetivamente visto no celular: a faixa Empresa ativa do menu mobile agora exibe uma seta funcional para troca de tenant quando o usuário é `saas_admin` e existem múltiplas empresas. Integrações foi removida da navegação e preferências antigas são migradas para Atendimento.

**Arquivos/commits:** informar o commit publicado após o push.

**Validação:** `git diff --check`, `npm run lint`, `npm test` (166 arquivos / 860 testes) e `npm run build` aprovados.

**Pendente ou risco:** confirmação visual no celular após o deploy; a troca segue exclusiva para `saas_admin`.
```
