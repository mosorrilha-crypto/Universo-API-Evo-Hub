# TASK-0089 — adiciona link do segundo catálogo (Beauty Concierge) na tela de Catálogo Público, exclusivo do tenant Monique

- **Iniciado em:** 2026-08-26T16:59:45.668Z
- **Agente/plataforma:** Claude
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** concluída

## Objetivo

adiciona link do segundo catálogo (Beauty Concierge) na tela de Catálogo Público, exclusivo do tenant Monique

## Atualizações

Depois de mesclar o segundo catálogo (TASK-0087), o usuário perguntou onde selecionar/acessar
esse segundo catálogo no painel — não havia nenhum ponto de acesso, só a URL direta. Adicionado
um segundo card em `src/components/PublicCatalogSettings.tsx`, visível apenas quando
`tenantSlug === 'monique-teste'` (constante `MONIQUE_SECOND_CATALOG_TENANT_SLUG`), com link pra
`/catalogo/monique-teste/novo`. Reaproveita o mesmo toggle "Catálogo público ativo" (os dois
catálogos leem do mesmo `public_catalog_enabled`), então o botão "Ver catálogo" do segundo card
só aparece quando `form.enabled` é true, igual ao card do catálogo original. Texto explicativo
do toggle atualizado pra citar as duas rotas quando é este tenant.

## Encerramento

**Validação:** `npm run lint`, `npx vitest run` (184 arquivos, 955 testes) e `npm run build`, todos limpos.

**Pendente ou risco:** nenhum no código; confirmar visualmente no painel que o card aparece só
pra Monique.

### Modelo de atualização na #290

```markdown
### TASK-0089 — Claude — 2026-08-26T16:59:45.668Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
