# TASK-0039 — Exibir links de configuração Meta para novos tenants e esclarecer campos obrigatórios

- **Iniciado em:** 2026-08-24T03:22:40.684Z
- **Agente/plataforma:** Manus AI
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** em revisão

## Objetivo

Exibir links de configuração Meta para novos tenants e esclarecer campos obrigatórios

## Atualizações

A captura confirmou que a `main` já tinha o campo de gerenciamento, mas não apresentava o link para novos tenants. O link foi adicionado no formulário inicial da Central de Tráfego junto com os links oficiais do Graph API Explorer, System Users e Access Token Debugger.

Uma segunda captura mostrou que o link precisava estar imediatamente abaixo do campo `Access Token Graph API` da seção CAPI, e não somente no cabeçalho do cartão. O link foi reposicionado/adicionado nesse ponto exato com o texto “Gerar este token no Graph API Explorer”, para ficar visível no primeiro acesso.

Como a captura também mostrou a tela CAPI sem orientação, foram adicionados nessa tela os links do Graph API Explorer e do Events Manager. O Test Event Code passou a ser identificado explicitamente como opcional e exclusivo para testes.

Os seis campos não são um único formulário obrigatório: a Central de Anúncios usa conta de anúncios, token de leitura e token de gerenciamento; CAPI usa Pixel ID e Access Token Graph API; Test Event Code é opcional. Nenhum token foi recebido ou armazenado durante a execução.

Arquivos alterados: `src/components/TrafficCenter.tsx` e `src/components/AdAttributionCAPI.tsx`.

Registre aqui decisões relevantes, arquivos alterados e bloqueios durante a execução.

## Encerramento

Ao concluir, atualize o status, informe os commits e as validações na issue #290 usando o identificador **TASK-0039**.

### Encerramento da implementação

**Tarefa concluída:** links oficiais de configuração foram adicionados aos fluxos de primeiro acesso da Central de Anúncios e da tela CAPI, com explicação dos campos opcionais. Na tela CAPI, o link de geração do token está diretamente abaixo do campo `Access Token Graph API`, conforme solicitado.

**Arquivos/commits:** `src/components/TrafficCenter.tsx` e `src/components/AdAttributionCAPI.tsx`. A PR #393 será atualizada com a correção de posicionamento.

**Validação:** `npm run lint`, `npx vitest run` — 166 arquivos e 860 testes —, `npm run build` e `git diff --check` aprovados. O build mantém apenas o aviso não bloqueante de chunk JavaScript acima de 500 kB.

**Pendente ou risco:** revisar humanamente a atualização da PR #393 e aguardar o deploy do Render. O link não aparecerá no navegador de produção antes do deploy.

### Modelo de atualização na #290

```markdown
### TASK-0039 — não informado — 2026-08-24T03:22:40.684Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
