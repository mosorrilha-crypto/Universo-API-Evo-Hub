# TASK-0031 — corrigir atomização visual da Central no Android

- **Iniciado em:** 2026-08-24T01:52:45.232Z
- **Agente/plataforma:** Manus.Ai
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** concluída

## Objetivo

corrigir atomização visual da Central no Android

## Atualizações

- A numeração original TASK-0030 colidiu com um registro já presente no `origin/main` (commit remoto `f33edcb`). O registro local foi renumerado para TASK-0031 antes da publicação; nenhum arquivo remoto foi sobrescrito.
- A TASK-0028 estava presente no bundle público, mas a captura continuava visualmente igual porque um bloco posterior de CSS, específico para dispositivos touch (`@media (hover: none) and (pointer: coarse)`), sobrescrevia a atomização anterior e voltava a empilhar o cabeçalho e a empresa ativa.
- O override final foi corrigido diretamente em `src/index.css`: cabeçalho em linha no Android, empresa ativa discreta e truncada à direita, indicadores em duas colunas e aviso de aprovação ocupando a largura completa.
- O service worker foi auditado e não é a causa: ele funciona em passthrough e não mantém cache offline de assets.
- A versão pública foi conferida após o deploy: o HTML atual carrega `index-COUcrbA6.js`, o marcador `Esperando você` está presente e o Render informa atualização pública em 23/08/2026 às 23:07:08 UTC.

## Arquivos alterados

- `src/index.css`
- `docs/task-registry/TASK-0031.md`

## Validação

- `git diff --check` passou.
- `npm run lint` passou (`tsc --noEmit`).
- `npm run build` passou para frontend e backend.
- O build mantém apenas o aviso não bloqueante conhecido de chunk JavaScript acima de 500 kB.
- A versão pública foi inspecionada por HTML e bundle após o deploy anterior; o novo CSS ainda precisa ser publicado para confirmar a aparência final no dispositivo.

## Encerramento

Ao concluir, informe os commits e as validações na issue #290 usando o identificador **TASK-0031**. Recomenda-se recarregar a aplicação no Android após o próximo deploy.

### Modelo de atualização na #290

```markdown
### TASK-0031 — Manus.Ai — 2026-08-24T01:53:50Z

**Tarefa concluída:** corrigido o override CSS tardio que anulava a atomização da Central no Android. O cabeçalho passa a manter Atendimento e empresa ativa na mesma linha compacta; os indicadores permanecem em duas colunas e a aprovação ocupa a largura completa.

**Arquivos/commits:** `src/index.css` e este registro.

**Validação:** `git diff --check`, `npm run lint` e `npm run build` passaram. A versão pública anterior já foi confirmada com o bundle contendo `Esperando você`; o novo CSS precisa ser publicado e depois conferido no celular.

**Pendente ou risco:** permanece o aviso não bloqueante de chunk JavaScript acima de 500 kB. Não houve alteração em dados, endpoints, permissões, service worker ou fluxo de mensagens.
```
