# TASK-0173 — Documentação embutida do painel de Disparo em Massa (pt/es) + link direto pra criação de template na Meta

- **Iniciado em:** 2026-08-30T17:14:04.718Z
- **Agente/plataforma:** Claude
- **Issue central:** [#504](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/504) (painel atual — #290 é histórico congelado, ver CLAUDE.md)
- **Status:** concluído

## Objetivo

Adicionar, dentro da própria página de Disparo em Massa (não como Artifact externo), um ícone de "Documentação" no mesmo padrão já usado na Base de Conhecimento (`KnowledgeBaseDocumentation.tsx`/`AgentKnowledgeBase.tsx`), explicando como o recurso funciona e como usá-lo — em pt/es conforme o idioma já selecionado no sistema (`AppPreferencesContext`) — e incluir um link direto pra página de criação de template na Meta Business Manager, com o passo a passo de como preencher cada campo do formulário da Meta em correspondência com os campos do nosso modal "Novo template".

## Atualizações

- Criado `src/components/BroadcastDocumentation.tsx`: componente bilíngue (pt/es), conteúdo em `CONTENT[language]` sem depender do arquivo global `i18n/translations.ts` (mesmo padrão de conteúdo grande hardcoded que `KnowledgeBaseDocumentation.tsx` já usa). Cobre: visão geral das 4 abas, tabela da curva de aquecimento, seção "Criar o template na Meta" com link para `https://business.facebook.com/wa/manage/message-templates/` (URL oficial de entrada do WhatsApp Manager, redireciona pro WABA já logado do usuário) e passo a passo mapeando cada campo do formulário da Meta (Categoria, Nome, Idioma, Cabeçalho, Corpo com variáveis numeradas, Botões) pro campo equivalente do nosso modal, passo a passo de uso completo, 3 caixas de regras de segurança (garantido pelo sistema / depende de quem opera / nunca faça) e FAQ.
- `src/components/BroadcastAdminPanel.tsx`: importa `useAppPreferences` (pra pegar `language`) e o novo componente; adicionado botão "Documentação"/"Documentación" (ícone `BookOpen`, mesmo estilo do botão já existente na Base de Conhecimento) no cartão de cabeçalho do painel; `showDocumentation` state faz o painel renderizar só a documentação (com botão "Voltar") em vez das abas, no mesmo padrão de `AgentKnowledgeBase.tsx`.
- Branch `claude/secure-contact-list-dispatch-l28z0j` reiniciada a partir do `main` atual antes deste commit, porque a PR anterior desta branch (#552, TASK-0171) já havia sido squash-merged — não dá pra empilhar em cima de histórico já mesclado.
- Documentação em Artifact publicada anteriormente (fora do código, sob pedido pontual do usuário) segue existindo à parte; esta tarefa é a versão **dentro do próprio painel**, que é o que o usuário pediu nesta rodada.

## Validação

`npm run lint`, `npx vitest run` (1221 testes) e `npm run build` rodados limpos antes do commit.

## Encerramento

Reportado na issue #504 com o identificador **TASK-0173**.

## Encerramento

Ao concluir, atualize o status, informe os commits e as validações na issue #290 usando o identificador **TASK-0173**.

### Modelo de atualização na #290

```markdown
### TASK-0173 — Claude — 2026-08-30T17:14:04.718Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
