# TASK-0010 — migrar catalogo da Monique para o Render e integrar cadastro de produtos do Universo

- **Iniciado em:** 2026-08-22T08:11:39.380Z
- **Agente/plataforma:** Manus.Ai
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** em andamento

## Objetivo

migrar catalogo da Monique para o Render e integrar cadastro de produtos do Universo

## Atualizações

- Inspecionado o catálogo público da Monique na Netlify: landing page estática, com serviços, preços, FAQs, Instagram e CTAs de WhatsApp.
- Escolhida a implantação no mesmo serviço Render do Universo, em `/catalogo/monique`, preservando a Netlify como rollback até a validação final.
- O catálogo público lê os produtos da `knowledge_base` real do tenant e expõe somente nome, categoria, descrição, preço vigente, moeda, duração e variantes. Regras da IA, aliases, documentos, vídeos e imagens privadas ficam fora do payload.
- A resolução pública usa `slug` no path e exige `public_catalog_enabled = true`; não aceita `tenant_id` em query/body. A leitura possui rate limit próprio de 60 requisições por minuto.
- A migration `0042_public_catalog.sql` adiciona o opt-in e os campos de contato público. Deve ser aplicada no Supabase antes do deploy/merge da nova rota, pois o conector Supabase desta sessão não está habilitado.

Arquivos previstos: `server/routes/publicCatalog.ts`, `server/services/publicCatalogStore.ts`, `server/middleware/rateLimit.ts`, `src/components/PublicCatalogPage.tsx`, `src/main.tsx`, `supabase/migrations/0042_public_catalog.sql` e testes correspondentes.

- O editor da Base de Conhecimento ganhou o atalho “Ver catálogo”, abrindo `/catalogo/:slug` em nova aba para conferência após o cadastro/edição de produtos.
- Validação local final: `npm run lint`, `npx vitest run` (143 arquivos, 770 testes), `npm run build` e `git diff --check` aprovados.

## Encerramento

Ao concluir, atualize o status, informe os commits e as validações na issue #290 usando o identificador **TASK-0010**.

### Modelo de atualização na #290

```markdown
### TASK-0010 — Manus.Ai — 2026-08-22T08:11:39.380Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
