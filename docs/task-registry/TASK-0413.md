# TASK-0413 — Remove código morto identificado em auditoria completa do repo: componentes nunca montados (FinancialDashboard, cluster FileUpload/AudioRecorder/TranscriptHistory/TranscriptionCard, WhatsAppGuide, ConversationMessageBubble), exports sem uso (getRecentResults, 5 funções de auth por email/senha do Firebase) e dependências órfãs (motion, recharts) — mantém KnowledgeBaseTypedDocumentsPanel fora, a pedido do usuário

- **Iniciado em:** 2026-09-14T14:56:43.579Z
- **Agente/plataforma:** Claude
- **Issue central:** [#758](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/758) (nota: `scripts/start-task.ts` ainda gera o link pra #504 no template — boilerplate desatualizado desde a rotação de 11/09/2026, ver nota igual na TASK-0412)
- **Status:** em andamento

## Objetivo

Remove código morto identificado em auditoria completa do repo: componentes nunca montados (FinancialDashboard, cluster FileUpload/AudioRecorder/TranscriptHistory/TranscriptionCard, WhatsAppGuide, ConversationMessageBubble), exports sem uso (getRecentResults, 5 funções de auth por email/senha do Firebase) e dependências órfãs (motion, recharts) — mantém KnowledgeBaseTypedDocumentsPanel fora, a pedido do usuário

## Atualizações

**Pedido original**: usuário pediu uma varredura no código do sistema pra achar linhas redundantes/não mais necessárias. Escopo escolhido (via pergunta direta): o sistema inteiro, não só as mudanças desta sessão.

**Método**: delegado a um agente de exploração (background) pra varrer `src/`+`server/` inteiros contra a árvore de renderização real (`App.tsx`), os routers montados (`server.ts`) e o `package.json`. Antes de apagar qualquer coisa, RE-VERIFIQUEI cada achado do agente com grep próprio — encontrei e descartei 2 falsos positivos do próprio relatório do agente (`FileUpload`/`AudioRecorder` pareciam ter matches em `AgentKnowledgeBase.tsx`/`WhatsAppLeadsSim.tsx`, mas eram só substring de nomes de função tipo `handleRealFileUpload` e um comentário mencionando "mesmo microfone do AudioRecorder.tsx" — não imports de verdade).

**Removido** (todos com zero call site confirmado por grep próprio, sem teste dedicado a atualizar):
- `src/components/FinancialDashboard.tsx` (782 linhas) — já documentado como morto no CLAUDE.md, reconfirmado.
- Cluster de "workspace de transcrição de voz", nunca ligado ao `App.tsx`: `FileUpload.tsx`, `TranscriptHistory.tsx`, `AudioRecorder.tsx`, `TranscriptionCard.tsx` (~995 linhas).
- `WhatsAppGuide.tsx` (319 linhas) — guia estático sem link de nenhuma aba.
- `owner-panel/ConversationMessageBubble.tsx` (200 linhas) — `WhatsAppLeadsSim.tsx` renderiza bolhas com JSX próprio.
- `getRecentResults()` em `server/services/transcriptionQueue.ts` — sem call site.
- 5 funções de auth por e-mail/senha do Firebase em `src/lib/firebase.ts` (`loginWithEmailPassword`, `registerWithEmailPassword`, `resetUserPassword`, `logoutFirebase`, `onAuthUpdate`) — sobras de um design anterior (login real hoje é cookie de sessão via backend; só `loginWithGoogle` continua em uso). Removidos junto os imports que ficaram órfãos (`signInWithEmailAndPassword`, `createUserWithEmailAndPassword`, `sendPasswordResetEmail`, `signOut`, `onAuthStateChanged`, `User`) e mais 5 imports que já estavam sem uso ANTES desta limpeza (`getDoc`, `collection`, `query`, `where`, `getDocs` de `firebase/firestore` — achado extra, não fazia parte do relatório original).
- Dependências `motion` e `recharts` do `package.json` (`recharts` só era usado pelo `FinancialDashboard.tsx` removido acima) — `npm install` removeu 42 pacotes transitivos.
- Dois comentários com referência cruzada apontando pro `FinancialDashboard.tsx` já morto (`src/App.tsx:1381`, `src/types.ts:78`) — corrigidos pra apontar pro componente real que faz esse trabalho hoje (`AgendaFinanceiroCenter.tsx`).

**Mantido fora, a pedido explícito**: `src/components/KnowledgeBaseTypedDocumentsPanel.tsx` — parece superseded pelo editor embutido em `AgentKnowledgeBase.tsx`, mas tem 2 arquivos de teste próprios e o usuário pediu pra confirmar com mais cuidado antes de mexer.

**Verificação**: `npm run lint` / `npx vitest run` (307/307 arquivos, 1772/1772 testes — nenhum teste dependia do código removido, nenhum precisou ser apagado/ajustado) / `npm run build` — todos limpos.

## Encerramento

Ao concluir, atualize o status, informe os commits e as validações na issue #504 usando o identificador **TASK-0413**.

### Modelo de atualização na #504

```markdown
### TASK-0413 — Claude — 2026-09-14T14:56:43.579Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
