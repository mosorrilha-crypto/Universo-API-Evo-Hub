# TASK-0215 — Mapa: pendencia Microsoft Clarity + fluxo geral unificado

- **Iniciado em:** 2026-09-02T17:32:00.211Z
- **Agente/plataforma:** Claude
- **Issue central:** [#290](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/290)
- **Status:** concluída

## Objetivo

Mapa: pendencia Microsoft Clarity + fluxo geral unificado

## Atualizações

Contexto: durante conversa sobre custos de infraestrutura (Render/Vercel), o dono
do produto perguntou sobre o Microsoft Clarity (heatmap/gravação de sessão,
gratuito) para as páginas públicas do catálogo/oferta. Confirmado no código que
existem rotas públicas reais e anônimas (`/catalogo/:slug` — `PublicCatalogPage.tsx`,
`/catalogo/monique-teste/novo` — `PublicCatalogMoniqueConcierge.tsx`, `/oferta` —
`PublicOfferPage.tsx`, roteadas em `src/main.tsx`) — superfície real onde o Clarity
agregaria valor (ver onde o lead desiste antes de chamar no WhatsApp). Ainda não
implementado; só documentado como pendência, conforme pedido.

Alterações em `docs/mapa-do-projeto/index.html`:
1. Nó tracejado "⏳ PENDENTE (TASK-0215) Microsoft Clarity" no fluxo 04
   (Marketing/Catálogo), ligado a `CAT` e `OFERTA`.
2. Nova linha "Analytics de comportamento nas páginas públicas" na tabela
   "Estado real por área" (seção 08), status pendência.
3. Nova seção 09 "Fluxo geral unificado" — diagrama Mermaid único conectando as
   cinco seções de fluxo (01 mensagem, 02 agenda/financeiro, 03 CRM/qualidade,
   04 marketing/catálogo, 05 admin/alertas) e o painel (07), mostrando como um
   lead nasce no marketing, vira conversa, pode virar agendamento ou escalonamento,
   como o "exemplo aprovado" realimenta a IA, e como administração/alertas vigia
   os quatro fluxos ao mesmo tempo. Item novo no sumário (`nav.toc`).
4. Badge de data atualizado para incluir TASK-0215.

`docs/GUIA-DO-PROJETO.md`: seção "Mapa visual (onboarding)" atualizada para
mencionar a nova seção 09 e a pendência do Clarity, mantendo a regra de
republicar o Artifact na mesma URL (obrigatório, TASK-0207).

Artifact republicado na mesma URL:
https://claude.ai/code/artifact/b7a564a3-024a-4b15-848a-c1323fbc1f4a

## Encerramento

`npm run lint` (tsc --noEmit), `npx vitest run` (240 arquivos / 1296 testes) e
`npm run build` rodados e passando limpo antes do commit — mudança é só de
documentação/mapa visual, nenhum código de aplicação foi tocado.

Pendente/risco: a implementação real do Microsoft Clarity (script + checagem de
CSP em `server.ts` + registro da conta) não foi feita nesta tarefa — só
documentada como pendência no mapa, como pedido explicitamente pelo dono do
produto ("coloca no artefato posicionado no fluxograma como pendente para fazer
no futuro").

Relatado na issue #504 (painel atual, não #290 — ver CLAUDE.md) com o
identificador **TASK-0215**.

### Modelo de atualização na #290

```markdown
### TASK-0215 — Claude — 2026-09-02T17:32:00.211Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
