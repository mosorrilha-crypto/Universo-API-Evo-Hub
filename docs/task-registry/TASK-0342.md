# TASK-0342 — Volta Status do agente e Idioma/Tema pra pills sempre visíveis (mais compactas), revertendo o colapso em ícone único da TASK-0336

- **Iniciado em:** 2026-09-08T04:51:51.538Z
- **Agente/plataforma:** Claude
- **Issue central:** [#504](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/504)
- **Status:** concluída — aguardando confirmação visual do dono do produto antes do merge

## Objetivo

Volta Status do agente e Idioma/Tema pra pills sempre visíveis (mais compactas), revertendo o colapso em ícone único da TASK-0336

## Contexto

Continuação direta da TASK-0340 (redução de tamanho dos botões da gaveta Ferramentas). Depois de eu mostrar uma prévia com o padrão atual (ícone único por ajuste, expande opções ao tocar — desenho da TASK-0336), o dono do produto mandou um print da versão AINDA em produção nesse momento (mais antiga que o código local, antes da TASK-0336 rodar) mostrando "STATUS DO AGENTE" e "IDIOMA E TEMA" como pills sempre visíveis, sem etapa de expandir — e pediu explicitamente "volta pra esse formato só que mais otimizado".

## Mudança

`src/components/WhatsAppLeadsSim.tsx`:
- Removido o estado `expandedQuickSetting` (só existia pra controlar qual ajuste estava expandido) e toda a lógica de tocar-pra-expandir de Status/Idioma/Tema.
- "Status do agente": volta a ser uma fileira de 3 pills sempre visíveis (Ativo/Restrito/Pausado, `flex-1` dividindo a largura igualmente), com rótulo de seção acima — mas bem mais compacta que a versão antiga de produção (padding `px-2 py-1.5`, texto `text-[11px]`, sem o ícone circular de 48px que existia antes disso na versão intermediária).
- "Idioma e tema": vira UMA fileira só combinando PT/ES + os 4 ícones de tema (antes eram 2 ajustes/ícones separados) — mesmo agrupamento mostrado no print de produção.
- "Somente anúncios" continua como ícone em círculo (só um toggle liga/desliga, sem opções pra escolher); "Gatilhos" (que só aparece quando anúncios está ativo) passou a ficar na MESMA fileira dele, em vez de numa fileira própria abaixo, igual ao agrupamento do print de produção.
- `src/components/__tests__/WhatsAppLeadsSim.toolsLanguageTheme.test.tsx` ajustado: não precisa mais clicar em "PT"/"Escuro" pra expandir antes de escolher outra opção — os botões já aparecem prontos.

## Encerramento

`npm run lint`, `npx vitest run` (276 arquivos / 1511 testes) e `npm run build` limpos.

**Arquivos:** `src/components/WhatsAppLeadsSim.tsx`, `src/components/__tests__/WhatsAppLeadsSim.toolsLanguageTheme.test.tsx`.

**Pendente ou risco:** mudança de UX em produção — enviei prévia visual (print estático com a mesma estrutura) pro dono do produto confirmar antes de abrir PR/mergear, mesmo padrão de cautela desta sessão pra mudanças visíveis ao usuário final. Não abrir PR até a confirmação chegar.

Ao concluir, atualize o status, informe os commits e as validações na issue #504 usando o identificador **TASK-0342**.

### Modelo de atualização na #504

```markdown
### TASK-0342 — Claude — 2026-09-08T04:51:51.538Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
