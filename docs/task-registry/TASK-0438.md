# TASK-0438 — Traduz as 6 seções de edição da Base de Conhecimento (AgentKnowledgeBase.tsx) pra pt/es

- **Iniciado em:** 2026-09-18T18:37:38.808Z
- **Agente/plataforma:** Claude
- **Issue central:** [#504](https://github.com/mosorrilha-crypto/Universo-API-Evo-Hub/issues/504)
- **Status:** concluído (fase 2 de N — ver "Pendente ou risco")

## Objetivo

Traduz as 6 seções de edição da Base de Conhecimento (AgentKnowledgeBase.tsx) pra pt/es

## Atualizações

Continuação direta da TASK-0436 (fase 1 — cabeçalho/grade/diagnóstico da tela). O usuário confirmou o escopo "o painel inteiro, de uma vez" mas, dado o tamanho real (~31 mil linhas, 41/51 componentes intocados), a entrega segue incremental por tela; esta fase fecha o resto do próprio `AgentKnowledgeBase.tsx` — as 6 seções de edição empilhadas (Perfil & Objetivo, Regras de Negócio, Preços & Produtos, FAQ, Documentos, Mensagem Inicial), os 2 modais (wizard de criação de produto, editor de horários) e o rodapé (banner de salvar/publicar, toast de sucesso, atalho pro WhatsApp Sim).

**Escopo traduzido**: cabeçalhos de seção (`h3`), parágrafos de descrição, rótulos de campo (não-placeholder), botões, badges/status, estados vazios, tooltips de ícone (`title`), e os textos dos 2 modais completos (wizard de produto: 3 passos, "Novo Produto ou Serviço"; editor de horários: "Horários de atendimento"). `WEEKDAY_LABELS` e `FIRST_CONTACT_BLOCK_META` (constantes de módulo, mesmo padrão da TASK-0436 com `TYPED_DOCUMENT_NAVIGATION`) trocaram `label` fixo por `labelKey`, resolvido com `t()` no ponto de uso — confirmado por grep que nenhum teste referenciava esses valores. `PRODUCT_WIZARD_STEPS` (constante local dentro do componente, com acesso ao hook) passou a chamar `t()` diretamente.

**Decisão de escopo (a mesma da fase 1, reafirmada)**: texto de `placeholder` dentro de inputs (dica cinza que some ao digitar, ex: "Ex: TechCorp CRM Solutions") ficou de fora — é majoritariamente dado de exemplo, não instrução de interface. **Deliberadamente NÃO traduzido nesta fase**: o editor profundo de produto/variante dentro da Seção 3 (tudo a partir do painel "Editar produto" quando um item é selecionado — nome/preço/categoria/duração inline, gestão de variantes, upload de foto/vídeo/antes-depois, promoção) e o componente `BeforeAfterEditor` reutilizado ali — é a parte menos visível numa demonstração (só aparece depois de clicar em um item específico) e, sozinha, é do tamanho de uma seção inteira (~270 linhas); fica como próxima fatia natural. Cabeçalho/descrição/filtros/lista/estados vazios da Seção 3 (o que aparece sem selecionar nenhum item) foram traduzidos normalmente.

**Arquivos/commits:** `src/i18n/translations.ts` (135 chaves novas × pt/es, total 163→298), `src/components/AgentKnowledgeBase.tsx`.

**Validação:** `npm run lint`, `npx vitest run` (320 arquivos / 1822 testes) e `npm run build` — todos limpos. **Sem validação visual** — mesma limitação já registrada na TASK-0436 (sandbox sem credenciais reais); recomendo abrir a Base de Conhecimento com o seletor em ES e revisar visualmente as 6 seções, os 2 modais e o rodapé antes da apresentação.

**Pendente ou risco:** ainda 100% em português fixo: o editor profundo de produto/variante da Seção 3 (maior bloco restante do próprio arquivo), `KnowledgeBaseDocumentation.tsx` (249 linhas) e `PromptAuditView.tsx` (226 linhas), e as ~40 telas restantes do sistema — `WhatsAppLeadsSim.tsx`/Atendimento (7.138 linhas) segue como a maior e mais crítica pra demonstração. Sem risco de regressão técnica identificado nesta fase.

## Encerramento

Ao concluir, atualize o status, informe os commits e as validações na issue #504 usando o identificador **TASK-0438**.

### Modelo de atualização na #504

```markdown
### TASK-0438 — Claude — 2026-09-18T18:37:38.808Z

**Tarefa concluída:** descreva o resultado.

**Arquivos/commits:** informe os links.

**Validação:** informe testes, build ou revisão manual.

**Pendente ou risco:** informe o que ainda precisa de confirmação.
```
